# Decision History

Status: current as of PR #34's merge (`seed_product_walkthrough` — local
product-walkthrough demo-data management command, with Architecture
Review remediation), `main` @
`35925feab428bf31e02d81359d3ff36b8aa0932c` (PR #34's merge commit).

This is **not** another ADR. It is an index of every ADR in the
repository — where to find it, what it decided, why, and whether it still
holds. Read the linked ADR for the full Context/Decision/Consequences
reasoning; this table exists so a new developer can scan the whole
decision history in one page before diving into any single document.

Dates are the month the underlying decision was actually made (verified
against `git log`), which for several ADRs predates the ADR *document*
being written — several of these were formally recorded during the
architecture-consolidation sprint (Module 18) to document decisions
already made in earlier modules. Where that's true, it's noted.

| Date | Decision | ADR | Reason | Current Status |
|---|---|---|---|---|
| 2026-07 | Architecture Freeze — 24 binding pre-code decisions | [ADR-001](../../build_architecture_records/ADR_001_ARCHITECTURE_FREEZE_v1_0.md) | Freeze the Person≠UserAccount≠Provider identity chain, `ServiceSupplier` abstraction, tenant isolation model, aggregate boundaries, and 15 other named things before any Django model existed, so 25 modules could be built independently without re-litigating the same questions. | **Active** — binding; any deviation requires a new, owner-approved ADR (ADR-001.23) |
| 2026-07 | Matching Engine ships a minimal, honest subset | [ADR-002](../adr/ADR-002_MATCHING_ENGINE.md) | The frozen Blueprint describes a matching subsystem depending on entities (`Request`, `CandidateResponse`, `CustomerSelection`) that don't exist yet. Rather than invent them prematurely, Matching persists only what it can support today (`MatchRound`/`MatchCandidate`) and defers the rest. | **Active** — the deferred pieces (customer selection, candidate response, RBAC enforcement) remain deferred; see `PROJECT_MODULE_STATUS.md` Module 02 |
| 2026-07 | API Foundation rebuilt on real Django REST Framework | [ADR-003](../adr/ADR-003_API_FOUNDATION_DRF.md) | An initial inspection checked only `pyproject.toml`, found no DRF dependency, and built a hand-rolled JSON API layer. This was wrong — `requirements/base.txt` (the real dependency manifest) already declared `djangorestframework` as intentional. Corrected mid-module. *(Formally documented in the Module 18 consolidation sprint, decision made in the API foundation module.)* | **Active** — standard API layer for the whole platform |
| 2026-07 | `apps.wallet` is the canonical wallet bounded context | [ADR-004](../adr/ADR-004_CANONICAL_WALLET_CONTEXT.md) | `apps.finance`'s earlier wallet (`WalletAccount`/`WalletTransaction`) was dormant scaffolding — nothing outside its own tests ever used it. Building a second, real wallet concept for real customer-wallet requirements would have duplicated it; instead the Finance version was marked legacy/frozen. *(Formally documented in the Module 18 consolidation sprint, decision made in the Wallet module.)* | **Active** — guardrail-enforced (`NoDuplicateWalletModelTest`); avoids duplicate wallet systems |
| 2026-07 | `PaymentIntent` (Payments) and `PaymentTransaction` (Finance) are separate bounded contexts, bridged by orchestration | [ADR-005](../adr/ADR-005_PAYMENT_INTENT_VS_FINANCE_PAYMENT_TRANSACTION.md) | `finance.PaymentTransaction` is a single-shot, already-succeeded settlement record. The Payment Gateway module needed a genuine multi-step orchestration state machine (create → attempt → callback → settle) that shape can't represent. Built as a new, upstream context instead of overloading the old one. *(Formally documented in the Module 18 consolidation sprint, decision made in the Payments module.)* | **Active — the two contexts remain separate; the bridge between them was built in Epic 03 Sprint 1** (`SettlementOrchestrationService`, PR #26, merged); see the Epic 03 Sprint 1 rows below and `GAP_ANALYSIS.md` |
| 2026-07 | Reporting is a pure, models-less read layer | [ADR-006](../adr/ADR-006_REPORTING_READ_MODEL.md) | Avoids introducing OLAP, a data warehouse, or a cached/materialized reporting layer before the product needs one. Every report is computed live via ORM aggregation over existing operational tables. *(Formally documented in the Module 18 consolidation sprint, decision made in the Reporting module.)* | **Active** — read-only reporting; no caching layer exists |
| 2026-07 | Service-layer ownership & the thin-controller rule, formalized with an automated guardrail | [ADR-007](../adr/ADR-007_SERVICE_LAYER_THIN_CONTROLLER_RULE.md) | Every module had independently converged on "business logic lives in services, not views" since Module 08, but the rule had never been written down or checked. A real violation was found and fixed during a Module 17B review, proving convention alone wasn't durable enough. | **Active** — enforced by `ApiViewOrmDisciplineTest`/`AdminPortalOrmDisciplineTest` at test time, not just by convention |
| 2026-07 | Demand-side domain model: one requester account, no `FamilyMemberProfile` | [ADR-008](../adr/ADR-008_DEMAND_SIDE_DOMAIN_MODEL.md) | Product clarification: a customer requesting care "for my father" is the same kind of account event as requesting care "for myself" — the care recipient is data, not a second account. Scopes a future, reusable `CareRecipient` entity and a future, invitation-based Order Share Link, explicitly ruling out a family-member account type. | **Active** — `CareRecipient` and Order Share Link were built in the Customer Experience Phase 1 sprint (see below); the underlying "one account, no `FamilyMemberProfile`" ruling remains binding |
| 2026-07 | Customer Experience Phase 1: reconcile `CareRecipient` with pre-existing `ElderProfile` by extending in place | — (no numbered ADR; decision recorded here and in `GAP_ANALYSIS.md`) | ADR-008 named a *future* `CareRecipient` entity without knowing `ElderProfile` (an older, pre-existing model already FK'd from `Order.elder_profile`) would still be present when that future arrived. Introducing a second, parallel model would have violated the sprint's own "no duplicate models" rule. Extending `ElderProfile` in place — new fields only, same class name — satisfies ADR-008's shape while reusing the existing FK and avoiding duplication. Product/UI vocabulary says "Care Recipient"; the Django model stays `ElderProfile`. | **Active** — `apps.accounts.models.profiles.ElderProfile`, `apps.accounts.services.care_recipients.CareRecipientService` |
| 2026-07 | Customer Experience Phase 1: extend the ADR-007 thin-controller guardrail to `apps.portal` | — (no numbered ADR; extends ADR-007) | A post-merge-review architecture pass found `apps/portal/views.py` calling the ORM directly in nine places (multi-row reads with no service in front of them) — exactly what ADR-007's `ApiViewOrmDisciplineTest`/`AdminPortalOrmDisciplineTest` exist to prevent, but no equivalent guardrail covered the new app. Fixed by adding `OrderQueryService`/`CatalogQueryService`/`OrderTimelineService` (`apps.orders`) and `NotificationQueryService` (`apps.notifications`), moving order-status timeline interpretation out of the portal layer into `apps.orders`, and adding `PortalOrmDisciplineTest`. | **Active** — enforced by `PortalOrmDisciplineTest` at test time, mirroring `AdminPortalOrmDisciplineTest` |
| 2026-07 | Epic 02: Provider Portal built supplier-generic, on `kernel.ServiceSupplier` — never `CaregiverProfile` directly | — (no numbered ADR; extends ADR-001's `ServiceSupplier` freeze) | `apps.provider_portal` must be reusable later for nurses, physiotherapists, technicians, and other supplier types without rewrite, per this epic's explicit constraint. `apps.accounts.services.provider_identity.resolve_supplier_for_user()` is the one, single call site that turns a `UserAccount` into a `ServiceSupplier`; the portal app itself imports no concrete profile model. | **Active** — `apps.provider_portal` has zero imports of `CaregiverProfile` (verified by grep); all identity resolution goes through `resolve_supplier_for_user()` |
| 2026-07 | Epic 02: provider assignment actions and organization assignment strategy are explicit, extensible service-level transition tables, not inline status writes | — (no numbered ADR; mirrors the pre-existing `apps.payments.services.transitions` pattern) | The epic required that future actions (counter-offer, delay request, escalation, replacement, and future assignment strategies like automatic/bulk/shift) be addable without breaking the existing contract. `apps.booking.services.provider_actions.ALLOWED_PROVIDER_TRANSITIONS` (one dict key per named action: `confirm`, `decline`) and `apps.booking.services.organization_assignment.OrganizationAssignmentService` (one named strategy, `assign_manual`, behind a service boundary designed to hold more) both follow this shape. Neither introduces a second assignment/booking engine — both call the unmodified `AssignmentService`/`ExecutionService`. | **Active** — `apps.booking.tests.test_provider_actions`, `apps.organization_portal.tests.test_assignment_center` |
| 2026-07 | Epic 02: `apps.provider_portal`/`apps.organization_portal` use ownership, not RBAC permission keys, as their security boundary | — (no numbered ADR; extends the `apps.portal` precedent) | The epic's suggested permission keys (`provider.assignments.act`, `organization.staff.read`, etc.) were not implemented literally as RBAC keys. `PermissionService.require()` calls would fail closed for any real actor without a seeded role assignment, and no infrastructure exists yet to seed org/provider-scoped `RoleAssignment.scope_type`/`scope_id` rows. Instead, `resolve_supplier_for_user()` (provider) and `resolve_organization()` (organization, via `list_administered_organizations(user).first()`) resolve "my own data" directly, mirroring `apps.portal.permissions`'s established pattern — a customer/provider/org-admin is always allowed to see their own data and never anyone else's. | **Active for the portal-level ownership boundary itself.** The `OrganizationAssignmentService.assign_manual()` half of this entry (actor attribution/audit labeling) was superseded by the Enterprise Architecture Review follow-up row below — it no longer calls `AssignmentService.assign()` with `assigned_by=None`/unattributed. |
| 2026-07 | Epic 02: assignment-confirm → execution-session-creation orchestration lives in `apps.provider_portal`'s view layer, not inside `apps.booking` | — (no numbered ADR; respects the existing one-way dependency graph) | `apps.execution` already imports from `apps.booking` (`apps.execution.services.session_service` imports `SupplierAssignmentStatus`), so `apps.booking` must never import `apps.execution` back. Confirming a provider assignment and creating the corresponding `ExecutionSession` are two calls the portal view makes in sequence, not a single service-layer call that would require crossing that boundary. | **Active** — `apps.provider_portal.views.assignment_confirm_view`; see `dependency-graph.md` |
| 2026-07 | Epic 02: `CustomerRequestSubmitted` was not added as a new domain event | — (no numbered ADR) | The epic's candidate event list named `CustomerRequestSubmitted` alongside several others. `apps.orders.services.order_creation.create_public_order()` already publishes `ORDER_CREATED` unconditionally on every order creation, with a full audit trail via the existing `DomainEvent`/`publish()` pipeline. A second event describing the same state transition (a new order entering the system) would have been a duplicate, not an addition. | **Active** — `ORDER_CREATED` remains the canonical event for order/request submission |
| 2026-07 | Enterprise Architecture Review follow-up: `resolve_admin_organization()` removed as dead code | — (no numbered ADR) | The review found `apps.accounts.services.organization_identity.resolve_admin_organization(user, organization_id)` had zero call sites — two other modules' docstrings claimed it was "the" ownership-verification entry point, but the real, tested one is `apps.organization_portal.permissions.resolve_organization()` (no-args, single-org-per-admin). Rather than wire in an unused id-based lookup the portal's own architecture deliberately never needs (it never accepts an organization id from the request), the function was deleted and both misleading docstrings corrected to name the real enforcement path. | **Active** — `list_administered_organizations()` remains the one exported resolver; an id-based lookup can be added if/when multi-organization switching is built |
| 2026-07 | Enterprise Architecture Review follow-up: `CareRecipientService.archive()` now publishes `CareRecipientArchived` | — (no numbered ADR) | `create()`/`update()` published `CareRecipientCreated`/`CareRecipientUpdated`; `archive()` — also a real state transition — published nothing, breaking the file's own stated audit-trail pattern. Fixed by adding the `CareRecipientArchived` event constant and publishing it from `archive()` via the same `transaction.on_commit` + `publish()` pattern as the other two methods — no parallel audit mechanism. | **Active** — `apps.accounts.tests.test_care_recipients.CareRecipientEventPublishingTest` |
| 2026-07 | Enterprise Architecture Review follow-up: `PermissionService.require()` gains `ownership_authorized_by` — a named, audited alternative to the `actor=None` system-context bypass | — (no numbered ADR; extends `PermissionService`, the sole RBAC evaluator per ADR-001.13) | `OrganizationAssignmentService.assign_manual()` called `AssignmentService.assign(assigned_by=None)`, which silently logged the action as `rbac.permission.system_context` even though a real organization admin initiated it — misrepresenting a human action as a system one, and leaving `SupplierAssignment.assigned_by`/`OrderStatusHistory.changed_by` unattributed. Building full organization-scoped RBAC seeding (new `Role`s, a membership-to-`RoleAssignment` sync mechanism, backfill) was judged too large and architecturally significant to safely improvise as a fix — it is real, separate, epic-worthy work, consistent with this epic's own rule that a major new cross-cutting mechanism needs its own inspection/report first. Instead: `PermissionService.require()` gained an additive `ownership_authorized_by` parameter (default `None`, zero behavior change for all 8+ existing call sites that don't pass it). When `actor is None` and `ownership_authorized_by` is given, that real actor is tried as a normal RBAC actor first (so the moment real seeding exists, this call is fully, normally enforced with no further code changes); only on RBAC failure does it fall back to an explicit `rbac.permission.ownership_authorized` audit entry, correctly attributed to that real actor — never `system_context`. `AssignmentService.assign()` gained the same passthrough parameter and now uses `assigned_by or ownership_authorized_by` for all attribution (the `SupplierAssignment` row, `OrderStatusHistory.changed_by`, every published event's `actor_id`). `OrganizationAssignmentService.assign_manual()` now passes `ownership_authorized_by=actor` instead of leaving `assigned_by` unset. | **Active, with a tracked follow-up** — see `GAP_ANALYSIS.md`'s "Organization-scoped RBAC seeding" entry for what would let this call site drop the ownership-authorized fallback entirely |
| 2026-07 | Epic 03 Sprint 1: `SettlementOrchestrationService` lives in `apps.payments.services`, not a new app | — (no numbered ADR; extends ADR-005 and the one-way dependency graph) | The orchestration needs to import `apps.finance`, `apps.wallet`, `apps.orders`, and (after remediation) `apps.jobs`. `dependency-graph.md` already documented `payments (→ finance, wallet)` before this sprint — `apps.payments` is downstream of both. Placing the service there wires the contexts ADR-005 separated without inverting the graph or introducing a new app for what is, per this sprint's own scope, pure wiring. `apps.orders` and `apps.jobs` were added as new import edges during the sprint — both graph-safe (`orders` already sits upstream of `finance`/`payments`; `apps.jobs` is dependency-free infrastructure, the same role `apps.kernel` plays at the graph's root). | **Active** — `apps.payments.services.settlement_orchestration_service`, `apps.payments.jobs`; `dependency-graph.md` updated |
| 2026-07 | Epic 03 Sprint 1: settlement *resolves* an existing `FinancialDocument`, it never creates one | — (no numbered ADR; respects `FinancialDocumentService`'s existing ownership of document creation) | The Sprint 1 brief required reusing existing document services and explicitly forbade redesigning invoicing. Deriving invoice line items from an order/quote inside the orchestration would have been exactly that — a second, competing path into `FinancialDocument` creation. Instead, `SettlementOrchestrationService` looks up the order's newest non-cancelled/voided `FinancialDocument`, issues it if still `DRAFT`, and raises a clear `SettlementError` (never a silent no-op) if none exists yet — a document must already have been created via `FinancialDocumentService.create_invoice_from_execution()` by some other trigger first. | **Active, with a documented limitation** — no production caller yet creates an `Order`-referencing `PaymentIntent` with a corresponding invoice wired end-to-end from a customer-facing checkout flow; see `GAP_ANALYSIS.md` |
| 2026-07 | Epic 03 Sprint 1: escrow-configured-but-unimplemented warns and falls back to Direct Settlement, it does not error | — (no numbered ADR; implements the Financial Settlement Architecture Specification's Escrow Policy) | An earlier draft of the settlement design proposed raising a hard error if `FinanceConfiguration.get_escrow_enabled()` was `True` but no escrow execution path existed, to avoid silently behaving incorrectly. This was deliberately overridden during architecture review: erroring would fail real customer payments over a configuration flag with no operational lever to fix it (escrow execution is out of Sprint 1's scope by design), which is worse than a monitored fallback. `SettlementOrchestrationService.settle_payment_intent()` logs a `WARNING` naming the tenant and `PaymentIntent`, then proceeds with Direct Settlement — money still moves. The warning is log-only (no persisted/alertable record) and re-fires on every retry attempt; tracked as technical debt (see `GAP_ANALYSIS.md`). | **Active, with tracked follow-up** — see `SettlementEscrowPolicyTest` in `apps/payments/tests/test_settlement_orchestration.py` |
| 2026-07 | Epic 03 Sprint 1: `SettlementAdjustmentPipeline` is a generic, named-adjustment extension point, not a `CommissionPolicy` | — (no numbered ADR; implements the Financial Settlement Architecture Specification's Commission Policy refinement) | The architecture review explicitly rejected a narrow commission-only abstraction in favor of one seam that future commission, tax, discount-recovery, and promotional-credit rules can all plug into without changing any caller. `SettlementAdjustmentPipeline.run()` returns a `SettlementAdjustmentResult` (gross, net, commission, tax, discount-recovery, a list of named `SettlementAdjustment` lines) — Sprint 1's implementation is the identity function (every component zero, net == gross), but `LedgerService.post_entries()`'s caller already branches on `commission_amount > 0` to post a third balancing `platform.commission.revenue` line, proven by a direct unit test rather than left as untested dead code. No internal arithmetic-consistency validation exists yet (tracked as technical debt). | **Active** — `apps.payments.services.settlement_adjustments`; real rules and internal invariants remain future work, see `GAP_ANALYSIS.md` |
| 2026-07 | Epic 03 Sprint 1: three ledger account codes reserved (`platform.cash.collected`, `provider.receivable.settled`, `platform.commission.revenue`) | — (no numbered ADR; `LedgerEntry.account_code` is a free-text field by design, no registry model exists) | `LedgerService.post_entries()` treats `account_code` as caller-supplied. Rather than invent a chart-of-accounts model (out of scope — "introduce new models only if absolutely necessary"), the three codes this sprint's balanced postings use are recorded here as the de facto convention: `platform.cash.collected` (DEBIT, platform party, gross), `provider.receivable.settled` (CREDIT, beneficiary party, net), `platform.commission.revenue` (CREDIT, platform party, reserved — only posted once a future adjustment pipeline returns `commission_amount > 0`). | **Active** — `apps.payments.services.settlement_orchestration_service` module constants |
| 2026-07 | Epic 03 Sprint 1 remediation: concurrent settlement attempts are serialized on a `PaymentIntent` row lock, backed by two database `UniqueConstraint`s | — (no numbered ADR; Architecture Review Critical Finding 2) | The original implementation's idempotency guard was a plain read-then-write check (`PaymentTransaction.objects.filter(...).first()`), with no row lock and no database constraint — a genuine TOCTOU race under concurrent settlement attempts. Fixed by moving `PaymentIntent.objects.select_for_update().get(...)` to the first line of `settle_payment_intent()`, before the existing-payment check, so a second concurrent call blocks until the first's transaction commits or rolls back. As a database-level backstop, `finance/migrations/0002_settlement_idempotency_constraints.py` adds partial `UniqueConstraint`s on `PaymentTransaction(tenant, provider_reference)` and `LedgerEntry(payment_transaction, account_code)`. Verified with a real multi-thread, multi-connection `TransactionTestCase` (`SettlementConcurrencyTest`), not merely asserted. The `LedgerEntry` constraint's current shape (missing `party`/`entry_type`) is not forward-compatible with future split-payment/multi-beneficiary accounting and is tracked as technical debt requiring generalization before that work begins. | **Active** — `apps.payments.services.settlement_orchestration_service.settle_payment_intent`; generalization tracked in `GAP_ANALYSIS.md` |
| 2026-07 | Epic 03 Sprint 1 remediation: a failed synchronous settlement enqueues a durable `payments.settlement.retry` job via the existing `apps.jobs` foundation, rather than only logging | — (no numbered ADR; Architecture Review Critical Finding 1; extends `apps.jobs`, Module 20/22) | The original implementation left `PaymentIntent=SUCCEEDED` with no `PaymentTransaction`/`LedgerEntry`/`WalletTransaction` and no recovery signal beyond a log line if settlement raised. Rather than invent a new retry mechanism, `PaymentCallbackService._trigger_settlement()` now calls the pre-existing `JobService.enqueue(job_type="payments.settlement.retry", idempotency_key=str(intent.id), ...)` on failure — idempotent via `JobDefinition`'s own database-unique `(tenant_id, job_type, idempotency_key)` constraint, with retry/backoff/dead-letter inherited for free from `JobService.execute_job()`/`run_due_jobs`. The handler (`apps.payments.jobs._retry_settlement`) simply re-invokes `settle_payment_intent()`, itself already idempotent. This is `apps.payments`'s first use of `apps.jobs` for real business work (previously only `apps.jobs.handlers`' own demo handlers were registered) — see the `apps.payments -> apps.jobs` edge in `dependency-graph.md`. A narrow crash window (between the callback's commit and the enqueue call succeeding) and the fact that `JobService.enqueue()` itself is not guarded by a nested try/except remain open, tracked as technical debt — a periodic reconciliation sweep would close both. | **Active, with tracked follow-up** — see `GAP_ANALYSIS.md`'s technical debt register |
| 2026-07 | Epic 04 Sprint 1: order-organization eligibility is an explicit junction model with no automatic/implicit grant rule | [ADR-009](../adr/ADR-009_ORGANIZATION_ELIGIBILITY_AND_SCOPED_RBAC.md) | The Assignment Center's "tenant-wide, not organization-scoped" gap (`GAP_ANALYSIS.md`) needed a real eligibility concept between `Order` and `OrganizationProfile`. A direct `Order.organization` FK was rejected (cannot represent zero/multiple eligible organizations without a breaking migration); a formal bidding/routing model was rejected as disproportionate (that's a separate, larger, out-of-scope feature); an implicit "single organization in a tenant is automatically eligible" default was rejected because direct inspection of `create_public_order()`/`create_operator_order()` found no existing business signal to derive one from. `OrderOrganizationEligibility` (`apps.orders.models`, sole writer `OrderEligibilityService`, guardrail-enforced) is the result — every row is an explicit, actor-attributed `grant()` call via the new `grant_order_eligibility` management command. | **Active** — `apps.orders.services.eligibility_service.OrderEligibilityService`, `apps.orders.services.queries.OrderQueryService.list_eligible_for_organization()` |
| 2026-07 | Epic 04 Sprint 1: `AssignmentService.assign()`/`replace()` acquire `Order.objects.select_for_update()` as their first statement | — (no numbered ADR; mirrors the Epic 03 Sprint 1 `PaymentIntent` row-lock pattern) | Concurrent assignment attempts against the same order previously had no database-level serialization. Row-locking the order first, before any eligibility/permission check or mutation, closes the same class of TOCTOU race the `PaymentIntent` lock closed for settlement — verified with a real multi-thread, multi-connection `TransactionTestCase` (`apps.booking.tests.test_concurrency`), not merely asserted. | **Active** — `apps.booking.services.assignment_service.AssignmentService` |
| 2026-07 | Epic 04 Sprint 2: `RoleAssignment.scope_type="organization"` gets its first production writer, `OrganizationRoleSyncService` | [ADR-009](../adr/ADR-009_ORGANIZATION_ELIGIBILITY_AND_SCOPED_RBAC.md) | `RoleAssignment.scope_type`/`scope_id` and `PermissionService._scope_matches()` existed and were correctly evaluated since Module 08, but nothing ever wrote a scoped row. `OrganizationRoleSyncService` (`apps.accounts.services.organization_rbac`) is now the sole writer, hooked into the two places an `ADMIN`-role `OrganizationMembership` transitions status, backed by a new partial `UniqueConstraint` on `RoleAssignment` for idempotent, concurrency-safe syncing. Only `OrgMembershipRole.ADMIN` is synced — no other role_type has an enforcement point to hold a permission for yet. The pre-existing `ownership_authorized_by` fallback is deliberately left in place — no flag-day lockout, and a `backfill_organization_role_assignments` command covers memberships that predate the mechanism. | **Active, with a tracked gap** — see the row immediately below and `GAP_ANALYSIS.md` |
| 2026-07 | Epic 04 Architecture Review: the three organization permission keys this writer grants are not yet checked by any real call site | — (no numbered ADR; Architecture Review Critical Finding, PR #28) | Direct cross-reference of every granted permission key against every real `PermissionService` call site found `AssignmentService.assign()`/`replace()` check the literal `"booking.assignment.assign"`, not the granted `organization.assignment.assign` key; `approve_membership()`/`suspend_membership()` have no permission check at all. Classified as required remediation, not a blocker: the system remains safe throughout (the `ownership_authorized_by` fallback is itself correctly scoped), but Epic 04's Sprint 2 goal of "organization-scoped RBAC activation" is not functionally achieved by real RBAC evaluation in isolation. Remediated within Epic 04 by correcting the affected documentation (`permission_keys.py`, `rbac-permissions.md`, `ADR-009`) to state this accurately rather than asserting enforcement that doesn't exist; the functional fix (wiring the three call sites to their keys) was judged appropriate to land via the already-implemented, already-tested, stacked Epic 05 branch rather than reopening Epic 04's own RBAC code. | **Resolved by Epic 05** (PR #29, merged) — see the rows below |
| 2026-07 | Epic 04 remediation: `kernel.0011`'s `AddConstraint` reverse operation fixed via `SeparateDatabaseAndState` + explicit schema-qualified `RunSQL` | — (no numbered ADR; Architecture Review Critical Finding, PR #28) | `RoleAssignment.Meta.db_table` places the table in a `kernel` Postgres schema via a schema-qualification hack. Django's plain `AddConstraint` reverse SQL for a partial `UniqueConstraint` names the index unqualified (`DROP INDEX IF EXISTS "uq_role_assignment_active_scope"`), which resolves against `search_path` (default `"$user", public`) rather than the `kernel` schema the index actually lives in — the drop silently no-ops (`IF EXISTS` swallows the "not found" error) while migration-state bookkeeping still records the migration as unapplied, breaking any subsequent re-apply. Fixed by keeping the same `AddConstraint` for Django's model-state tracking while replacing the actual database SQL with schema-qualified `RunSQL` in both directions. Verified with a full apply/rollback/re-apply cycle against a freshly created database. | **Resolved** — `apps/kernel/migrations/0011_role_assignment_active_scope_constraint.py` |
| 2026-07 | Epic 04 Sprint 3: `SupplierType.ORGANIZATION_PROVIDER` becomes reachable for organization-affiliated caregivers | [ADR-009](../adr/ADR-009_ORGANIZATION_ELIGIBILITY_AND_SCOPED_RBAC.md) | `SupplierType.ORGANIZATION_PROVIDER` existed as an enum value since Module 03 but nothing ever created a supplier with it. `apps.accounts.services.supplier_bridge.get_or_create_supplier_for_caregiver()` now branches on `CaregiverProfile.provider_type`, creating this type for an `ORGANIZATION_AFFILIATED` caregiver. Financial policy is unchanged and verified by regression test: `FinancialPartyService.resolve_party_for_supplier()` still keys strictly on `supplier_type == SupplierType.ORGANIZATION`, so an affiliated caregiver's earnings continue to settle to their own wallet, never the organization's. A caregiver affiliated before this change keeps their existing `INDEPENDENT_PROVIDER`-typed supplier until the new `reconcile_organization_provider_suppliers` command is run (`SupplierRegistry.get_or_create_supplier()`'s `defaults` only apply on first creation) — the new generic `SupplierRegistry.set_supplier_type()` method performs the reconciliation. | **Active** — `apps.accounts.services.supplier_bridge`, `apps.kernel.services.supplier_registry.SupplierRegistry.set_supplier_type()` |
| 2026-07 | Epic 05: a lightweight, in-memory Python permission-key registry is authoritative; `kernel.Permission` stays dormant | [ADR-010](../adr/ADR-010_CANONICAL_PERMISSION_REGISTRY.md) | Permission-key strings were scattered as literals across service modules or lived in three independent, uncoordinated per-app `permission_keys.py` modules, with no validation anywhere (no duplicate/malformed-key detection, no proof a granted key is ever checked). A pre-existing, migrated `kernel.Permission` model already existed but had zero production writers and zero runtime readers. Rather than populate that model (a second source of truth to keep in sync, for a model with no runtime consumer — the exact dual-authority problem the System Architect's guidance warned against), `apps.kernel.permissions.registry.PermissionRegistry` — a plain in-memory dict, populated once via `KernelConfig.ready()` — became authoritative: 23 keys, each validated for format and uniqueness at Django startup (a malformed/duplicate key fails startup, not a runtime request). Existing per-app `permission_keys.py` modules became re-export facades over it; new facades added for `apps.booking`/`apps.finance`/`apps.execution`. `kernel.Permission` remains unchanged and dormant, left open to a future read-only projection if a real operational need (e.g. an admin-portal permission browser) ever emerges. | **Active** — `apps.kernel.permissions`, `apps.kernel.apps.KernelConfig.ready()` |
| 2026-07 | Epic 05: migrating every real `PermissionService` call site to a canonical constant surfaced three authorization defects, fixed with dedicated tests | [ADR-010](../adr/ADR-010_CANONICAL_PERMISSION_REGISTRY.md) | Tracing exactly which string each call site checks against exactly which string each role grant contains — forced by the registry migration, not previously done — found: (1) the phantom `organization.assignment.assign` key Epic 04 granted to `organization_admin` but `AssignmentService.assign()` never checked (it checks the literal `booking.assignment.assign` regardless of `scope`) — retired the phantom key, granted the canonical `BOOKING_ASSIGNMENT_ASSIGN` instead; (2) `OrganizationStaffService.approve_membership()`/`suspend_membership()` had granted keys with zero enforcement — added the check to both, mirroring `assign()`'s exact `ownership_authorized_by` fallback shape; (3) `AssignmentService.replace()` had no authorization of any kind — added it (no production caller existed yet, closed before one is wired up). None of the three would have been found by review of any single file in isolation. | **Active** — `apps.accounts.permission_keys`, `apps.accounts.services.organization_staff`, `apps.booking.services.assignment_service` |
| 2026-07 | Epic 05: a fourth, previously tracked defect (reviewer-ownership) also fixed under this Epic's explicit scope | [ADR-010](../adr/ADR-010_CANONICAL_PERMISSION_REGISTRY.md) | `ReviewSubmissionService.submit_review()` never verified `reviewer_person_id` was actually the order's own `customer_profile.person_id` — any authenticated user in the tenant holding `reviews.submit` could review any completed order, not just their own. Identified and documented before this Epic (`technical-debt-register.md`) but deliberately left unfixed at the time because that module's scope was API plumbing, not domain-service authorization changes. Epic 05's approved scope explicitly named this defect and authorized fixing it if small, well-specified, and dependency-safe — it was: a single service-layer check, no new model, no new permission key, enforced before both the duplicate-review check and `Review` persistence. | **Active** — `apps.reviews.services.review_submission_service.ReviewSubmissionService.submit_review()` |
| 2026-07 | Epic 05: `PermissionService._scope_matches()` scope-evaluation hardening | [ADR-010](../adr/ADR-010_CANONICAL_PERMISSION_REGISTRY.md) | An unscoped (`scope=None`) check previously matched *any* `RoleAssignment` regardless of that assignment's own `scope_type` — an organization-scoped grant also silently satisfied a platform-wide check. Not exploitable by anything that existed before this Epic (every organization-scoped grant is new in Epic 04, and every organization-isolation call site already passes an explicit `scope`), but closed here rather than left for a future caller to inadvertently depend on. Also hardened: explicit fail-closed handling for a malformed `scope` dict (missing keys) and a malformed `RoleAssignment` row (real `scope_type`, null `scope_id`) — both previously failed only incidentally. `RoleAssignment`/`Role`/`PermissionService`'s public evaluation contract (`check()`/`require()`) is otherwise unchanged. | **Active** — `apps.kernel.services.permission_service.PermissionService._scope_matches()` |
| 2026-07 | Epic 05: two independent role-seeding commands centralized into one shared catalog module, deliberately not merged into one taxonomy | [ADR-010](../adr/ADR-010_CANONICAL_PERMISSION_REGISTRY.md) | `apps.kernel.management.commands.seed_tenant` (hyphenated slugs, `dev`-slug bootstrap tenant) and `apps.accounts.management.commands.seed_auth_roles` (underscored slugs, the real default `salmandyar` tenant) cover genuinely distinct, only partially-overlapping role sets — not the same taxonomy with cosmetic naming differences. Merging them into one renamed taxonomy would mean renaming live `Role`/`RoleAssignment` database rows, a destructive operation out of this Epic's scope. `apps.kernel.role_catalog` is the reconciliation this Epic does make: both commands' role definitions now live in one shared module, permission keys validated against the canonical registry, and the one clear-cut divergence (`"platform-owner"` vs `"platform_owner"`) recorded as a known, deliberate, not-yet-resolved alias. New `reconcile_role_permissions` operational command (additive by default, tenant-scoped, idempotent) keeps each catalog's own roles in sync with its own canonical permissions. | **Active** — `apps.kernel.role_catalog`, `apps.kernel.management.commands.reconcile_role_permissions` |
| 2026-07 | Epic 05 Architecture Review remediation: the `ownership_authorized_by` security contract is now explicitly documented | — (no numbered ADR; Architecture Review Major Finding M1) | `ownership_authorized_by`'s actual behavior was correct and unchanged since Module 08/Epic 02, but no document stated plainly that it is not, on its own, a standalone authorization boundary — `PermissionService` cannot independently verify that the actor passed as `ownership_authorized_by` truly owns/administers the resource in question; it trusts the caller (e.g. `apps.organization_portal.permissions.resolve_organization()`) to have already verified that upstream. Passing the wrong actor is a caller bug, not a `PermissionService` bug. Documentation-only fix (no behavior changed, confirmed by an identical full-suite test count before and after) — added to `PermissionService`'s own module docstring and a dedicated `rbac-permissions.md` section. | **Active** — `apps.kernel.services.permission_service.PermissionService`, `docs/architecture/rbac-permissions.md` |
| 2026-07 | PR #32: `kernel.0010_useraccount_email_unique` ordering and rollback fixed, mirroring `kernel.0011`'s fix | — (no numbered ADR; reported directly against a fresh clone, not surfaced by any Epic) | Two independent defects in one migration. (1) Ordering: the data-cleanup `RunPython` step (`email="" → NULL`) ran *before* the `AlterField` granting `null=True`, so any database with a pre-existing blank-email `UserAccount` row (a normal state for real usage) aborted with an `IntegrityError`; invisible in CI because the test database is always created empty, so the cleanup step's `UPDATE` always matched zero rows there. Fixed by splitting into three ordered steps: allow `NULL` → convert data → add the unique constraint. (2) Rollback: independently confirmed the *original* migration's single combined `AlterField` also could never be cleanly rolled back — `UserAccount.Meta.db_table`'s `kernel`-schema-qualification hack breaks Django's built-in constraint/index introspection for `AlterField`'s reverse path the same way it broke `AddConstraint`'s reverse path for `kernel.0011` (`search_path` does not include `kernel`, so the unique constraint and LIKE index are silently never dropped, and a subsequent re-apply fails with "already exists"). Fixed the same way as `0011`: `SeparateDatabaseAndState` with explicit, schema-qualified `RunSQL` in both directions. The existing migration file was corrected in place — no new migration, no model change; Django tracks applied migrations by name only, so already-migrated databases are unaffected (independently verified). Architecture Review: APPROVED, no Critical/Major findings; one Minor, explicitly non-blocking finding — see the row below. | **Resolved** — `apps/kernel/migrations/0010_useraccount_email_unique.py` |
| 2026-07 | PR #32 Architecture Review Minor finding: no preflight check for pre-existing duplicate non-blank emails | — (no numbered ADR; Architecture Review Minor finding, non-blocking) | `email` was never unique before `kernel.0010` — not at the DB level, and `UserAccountManager.create_user()` never checked for an existing match either. It is therefore plausible (not confirmed) that a sufficiently-used pre-`0010` database already contains two rows sharing the same non-blank email; if so, `kernel.0010`'s `ADD CONSTRAINT UNIQUE` step fails with a duplicate-key `IntegrityError` (a safe, transactional abort, not data corruption) but with no operator-facing guidance beyond Postgres's raw error. Not a regression — the *original* migration had the identical exposure. Explicitly classified as out of scope for PR #32 (reconciling real duplicate user data is a product/data decision, not a mechanical migration fix) and not required for merge. | **Tracked, not yet resolved** — see `technical-debt-register.md` |
| 2026-07 | PR #34: `seed_product_walkthrough`, a local-development-only demo-data management command | — (no numbered ADR; local developer tooling, no architecture rule change) | No existing tooling let a developer browse the built customer/provider/organization/admin experiences without hand-seeding data through the UI. `apps.kernel.management.commands.seed_product_walkthrough` populates a dedicated, `DEBUG`-gated `demo-senior-platform` tenant with a full deterministic dataset entirely through existing service-layer writers (`ensure_customer_profile`/`ensure_caregiver_profile`, `CareRecipientService`, `OrganizationRoleSyncService`, `supplier_bridge.get_or_create_supplier_for_caregiver`, `OrderEligibilityService`, `AssignmentService`/`OrganizationAssignmentService`, `ExecutionService`, `FinancialDocumentService`/`FinancialPartyService`/`PaymentIntentService`/`PaymentCallbackService`/`WalletService`, `ReviewSubmissionService`) — no model is written to directly outside an approved writer, no UI/route/template/model/migration was touched. Settlement runs through the repository's own `FakePaymentProviderAdapter` (the tenant's real default provider), so no real SMS/email/push/payment call is possible. `--reset-demo` deletes and rebuilds only rows scoped to the demo tenant, verified to never touch another tenant's data. | **Active** — `apps.kernel.management.commands.seed_product_walkthrough` |
| 2026-07 | PR #34 Architecture Review remediation: three seed-command idempotency defects, plus a fourth found while verifying the fix | — (no numbered ADR; Architecture Review Major findings M1–M3, PR #34) | (1) **First-run execution completeness**: `_ensure_execution_and_finance()` evaluated a stale in-memory `Order` object's `.status`, not re-fetched after `_ensure_assignments()` mutated the underlying database row via `AssignmentService.assign()` — the in-progress example order's `ExecutionSession` was silently skipped on the very first run, only "catching up" a run later. Fixed by re-fetching the row (`Order.objects.get(id=...)`) immediately before evaluating it. (2) **Revoked-eligibility idempotency**: the revoked-eligibility example unconditionally ran `OrderEligibilityService.grant()` then `.revoke()` on every re-run — each a real, audited, published state transition — producing unbounded `AuditLog`/`EventOutbox` growth even though the final state never changed. Fixed with a pre-check guard that only runs the transition sequence when the row isn't already `WITHDRAWN`. (3) **Financial-party event idempotency**: `FinancialPartyService.resolve_party_for_supplier()` was called unconditionally for every demo provider on every run; that service publishes `Finance.Party.Resolved.v1` on every invocation by design (correct for its real production callers), so calling it unconditionally from a seed script produced unbounded event growth. Fixed with a new `_ensure_supplier_financials()` helper that mirrors the service's own lookup key exactly and only calls the resolver/`WalletService.create_wallet()` when the party or wallet doesn't already exist — `FinancialPartyService` itself was not modified, per the Architecture Review's explicit instruction to fix the caller. (4) A closely related, self-discovered fourth fix: `OrganizationRoleSyncService.sync_for_membership()` is called unconditionally in `_ensure_organizations()` and, like above, unconditionally audits on every invocation — a pre-check guard comparing the existing scoped `RoleAssignment` against the desired state was added, satisfying the same AuditLog-stability requirement. Verified via a real three-run, live-database snapshot diff (not the command's own counters) during both Architecture Re-Review and Acceptance Review: run 1 creates the complete dataset, runs 2 and 3 are byte-identical to each other across every tracked model, including per-row eligibility `granted_at`/`revoked_at` timestamps. 14 new regression tests added (27 total in the command's test file). | **Resolved** — `apps/kernel/management/commands/seed_product_walkthrough.py` |

---

## Where each ADR lives

- **ADR-001** — `build_architecture_records/ADR_001_ARCHITECTURE_FREEZE_v1_0.md` (repository root level, pre-code freeze package — not under `docs/adr/` because it predates the `docs/` structure).
- **ADR-002 through ADR-008** — `docs/adr/` (as-built decisions, made during implementation).

## Reading order

New to this decision history? Read them in the table's date order — each
one either narrows or corrects something ADR-001 froze, and later ADRs
sometimes reference earlier ones (ADR-008 assumes the multi-role account
model that ADR-001's identity chain made possible; ADR-005 assumes the
`FinancialParty` abstraction ADR-004 relies on).

For what's actually built against each of these decisions today, see
[`PROJECT_MODULE_STATUS.md`](PROJECT_MODULE_STATUS.md). For what's still
open because of them, see [`GAP_ANALYSIS.md`](GAP_ANALYSIS.md).
