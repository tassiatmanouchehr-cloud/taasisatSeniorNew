# DEFECT AND RISK REGISTER

**Last verified HEAD:** a5dbaf28703142edaa1d770ea8f3c2a45a12640f (full sweep); re-evaluated for
Phase 3 closure approval / Phase 4 Customer Portal Architecture Assessment (a code-free
governance/readiness review, not a numbered sprint), recorded via PR #15 and **MERGED to
main @ `078e435fee2b2c6350c66be113c4e7e607178763`** (2026-07-16) — no new defect found;
KL-021 (already RESOLVED) confirmed accurate against the current template; KL-022
re-evaluated, still open, now explicitly scoped as cross-portal
infrastructure (see `traceability/ARCHITECTURE_DECISION_LOG.md` ADM-026); re-evaluated again
for the Phase 4 Closure Review (documentation-only, `main` @
`756c14dc25d9446eff73b209bfd85b3e0f4c6648`, 2026-07-17) — KL-022 remains open and does not
block Phase 4 closure; two new non-defect Sprint 4.1 engineering-improvement entries added
(KL-023, KL-024, see below), neither a Phase 4 closure blocker nor a Phase 5 prerequisite
(see `traceability/ARCHITECTURE_DECISION_LOG.md` ADM-028); re-evaluated 2026-07-18 for the
Core Profile-ServiceSupplier Invariant Remediation — new FR-014 finding added (root cause of
a live "empty public directory" bug report). An independent pre-merge review of PR #18 (also
2026-07-18) found FR-014's initial RESOLVED marking premature: `resolve_supplier_for_user()`
still granted an ACTIVE `ServiceSupplier` to a never-activated DRAFT `CaregiverProfile`
reachable through ordinary `provider_portal` navigation, and the already-ACTIVE idempotent
activation path did not repair a missing/drifted supplier. Both were fixed on the same
branch (identity resolution split into a supplier-required path that now rejects non-ACTIVE
profiles, and a non-creating context-resolution path for self-view pages; the idempotent
activation path now reconciles the supplier without a duplicate transition/audit entry) and
independently re-verified — see `traceability/ARCHITECTURE_DECISION_LOG.md` ADM-029's
remediation addendum. FR-014 is RESOLVED again, this time with that verification recorded. A
second independent focused review of the remediation (2026-07-18) returned `APPROVE`; **PR #18
merged to `main`** (merge commit `096d7c50d2b912b52ae60468653cc2ce0df8798a`, final PR head
`3d723cbcd1237aa2ba24a151aabc9b9cff80bb20`, 2026-07-18) — FR-014 is now merged and closed.
Re-evaluated 2026-07-18 during manual runtime validation of PR #18's own outcome (the immediate
next task PR #18 itself set): the public caregiver directory returned zero results for a real,
eligible, seeded caregiver under an explicit tenant hint even though that same caregiver's
direct public detail page rendered correctly — new **FR-015** finding, root-caused to
`find_a_caregiver()` never resolving the `?tenant=` hint the sibling detail view already
resolved (a pre-existing view-layer gap, unrelated to PR #18's own changes). Fixed on branch
`fix/public-caregiver-directory-visibility` (PR #19). The same investigation proved the
identical gap exists in `find_an_organization()` — masked only by the unrelated seed-data
condition that `seed_product_walkthrough`'s organizations remain unverified — and fixed it
symmetrically on the same branch/PR rather than leaving a known, proven defect unresolved.
**PR #19 merged to `main`** (merge commit `aac018575a2f9380d75a11c55bf08b34ce65a511`, 2026-07-18)
after an independent final pre-merge review returned `APPROVE`. Re-evaluated again 2026-07-19
during manual runtime validation of PR #19's own merged outcome: FR-015's fix correctly made
the directories themselves tenant-aware, but every URL those directories *generate* (card
links, pagination, the filter form, "clear filters") still dropped the hint — a caregiver card
click from a tenant-hinted directory 404'd. New **FR-016** finding, a distinct root cause at
the template/URL-building layer (not the tenant-resolution layer FR-015 closed). Fixed on
branch `fix/public-directory-tenant-link-propagation`; see FR-016 below. **PR #20 merged to
`main`** (merge commit `e1f0bfd6c921636bab0c196eaf469d2eb2a667aa`, 2026-07-19) after an
independent final pre-merge review found and fixed one latent (unreached-by-any-caller)
robustness gap in `common.append_tenant_query()` during the review itself, then returned
`APPROVE`. Re-evaluated again 2026-07-19 during manual runtime validation of PR #20's own
merged outcome: `/find-a-caregiver/` (the bare public root, exactly what an ordinary visitor
types) still showed zero caregivers, while `/find-a-caregiver/?tenant=demo-senior-platform`
showed the correct real ones — a *third*, distinct root cause: `home()` never resolved any
tenant context at all (always the platform default tenant, unconditionally, not even
accepting an explicit hint), and the platform default tenant's own seeded suppliers are
ACTIVE but `unverified` (so `is_publicly_visible_attrs()` correctly hides them — a legitimate
data-verification gate, not a bug). There was no configuration mechanism for a deployment to
serve a *different*, already-populated tenant as its public site. New **FR-017** finding —
introduces `apps.public_site.services.tenant_context.resolve_public_tenant()`, a single
canonical resolver (explicit `?tenant=` hint → `settings.PUBLIC_SITE_TENANT_SLUG` if
configured → platform default, unchanged) used by every public view and a new context
processor so shared navigation chrome carries the resolved tenant too. Fixed on branch
`fix/public-site-canonical-tenant-resolution`; see FR-017 below. PR #21 merged (merge commit
`5e4364c868ea4082f40bd4588089951a8c81f247`). A subsequent code-and-runtime-backed audit of the
whole public site (not a numbered sprint — a coherence/usability audit) found 5 material,
reproducible defects (PSA-001 through PSA-005: generic page-title suffix, missing favicon, a
caregiver-profile CTA that lost the selected caregiver's context, a misleading unfiltered
organization empty state, and a raw unbranded Django 403 page) plus several low-risk polish
items (non-absolute Open Graph URL, a duplicated brand string in social-preview titles, a
shipped internal build comment, dead `href="#"` social placeholders). Remediated as **FR-018**
— see below. Investigating PSA-005's fix surfaced that every browser-facing portal already has
its own established, redundantly-tested non-disclosure policy (anonymous access returns a
plain 403, never a login redirect) — the fix respects that discovered policy rather than
overriding it. Re-evaluated again 2026-07-19 for a report that a healthy-looking
`CaregiverProfile` count coexisted with a zero-result `/find-a-caregiver/`: every directory
filtering/visibility predicate was individually re-verified and found correct (including the
one deliberately-suspended demo caregiver); the initial diagnosis was that FR-017's own
no-hint fallback correctly resolves the platform default tenant, which the dedicated
`seed_product_walkthrough` tenant is never meant to be — first closed only by making the seed
command's own report print the real directory URLs and warn about `PUBLIC_SITE_TENANT_SLUG`,
with no resolution code changed. **An independent corrective review rejected that as
insufficient** ("operational documentation, not a product-level resolution") and required the
canonical `/find-a-caregiver/` URL to work with zero manual `.env` edit. Corrected the same day
by giving the seed and the public resolver an actual shared, code-level canonical-tenant
contract (`apps.kernel.dev_tenant.CANONICAL_DEV_TENANT_SLUG`, consulted by
`resolve_public_tenant()` only when `settings.DEBUG=True` — structurally unreachable in
production/testing, both of which hardcode `DEBUG=False`) — proven end-to-end against a
completely clean process environment with no `.env` file at all. The same investigation found
two independent, genuine code defects (directory cards built via raw string concatenation
instead of Django's named route; a caregiver's own uploaded avatar never reaching any public
view, unlike the organization side's `logo_url`) and a seed-data completeness gap (zero
skills/experience/gallery/avatar on any of the 11 demo caregivers, despite the public profile
page fully supporting all four since Phase 2.1–2.3). All fixed as **FR-019** — see below.
**Phase 5 has not started.**
**Last verified date:** 2026-07-14 (full sweep); 2026-07-16 (targeted re-evaluation); 2026-07-17 (Phase 4 Closure Review); 2026-07-18 (Core Profile-ServiceSupplier Invariant Remediation + independent pre-merge review remediation + PR #18 merge + FR-015 caregiver-directory and organization-directory tenant-hint fixes + PR #19 merge); 2026-07-19 (FR-016 directory-link tenant-context propagation fix + PR #20 merge + FR-017 canonical public-site tenant resolution + PR #21 merge + public-site coherence audit + FR-018 remediation + PR #22 merge + FR-019 public caregiver marketplace remediation)

---

## CRITICAL Findings

### FR-001: No Automated Tenant Isolation at ORM Level

**Severity:** CRITICAL
**Confidence:** HIGH
**Affected:** All apps
**Evidence:** `TenantScopedManager.for_tenant()` is opt-in, not default. `TenantAwareModel.tenant_id` is a UUIDField, not a ForeignKey. No middleware injects tenant_id.
**Runtime impact:** A single forgotten `tenant_id` parameter in a new service creates a cross-tenant data leak.
**Why it matters:** Multi-tenant isolation is the foundational security guarantee. Currently enforced only by developer discipline.
**Suggested action:** Consider middleware-based tenant injection or row-level security (PostgreSQL RLS).

### FR-002: RBAC Enforcement Can Be Disabled Per-Tenant

**Severity:** CRITICAL
**Confidence:** HIGH
**Affected:** All permission-gated operations
**Evidence:** `apps/kernel/services/permission_service.py:135` — if `RBACConfiguration.get_enforcement_enabled()` is False, `require()` returns immediately.
**Runtime impact:** Setting `rbac.enforcement.enabled=false` in ConfigurationValue table disables ALL RBAC for that tenant.
**Why it matters:** No audit alert when enforcement is toggled. A compromised config change silently bypasses all permissions.
**Suggested action:** Add audit logging when enforcement is toggled. Consider removing the toggle in production.

---

## HIGH Findings

### FR-003: ownership_authorized_by Bypass in PermissionService

**Severity:** HIGH
**Confidence:** HIGH
**Affected:** booking, execution, commission services
**Evidence:** `permission_service.py:157-188` — if `ownership_authorized_by` is set and actor has no RoleAssignment, authorization is granted. PermissionService trusts the caller.
**Runtime impact:** Any caller can bypass RBAC by passing `ownership_authorized_by`. Security depends on callers being correct.
**Why it matters:** Defense-in-depth violation. Service-level auth depends on caller integrity.

### FR-004: FakeProviderCallbackView Unauthenticated

**Severity:** HIGH
**Confidence:** HIGH
**Affected:** payments
**Evidence:** `api/views/payments.py:72-117` — no authentication. Queries `PaymentAttempt` by `provider_reference` without tenant scoping.
**Runtime impact:** Anyone with a valid `provider_reference` can trigger payment callbacks. Protected only by unguessable token.
**Why it matters:** Real PSP webhooks need signature verification. Currently mocked.

### FR-005: Pre-Existing Seed Test Random order_number Collision — **RESOLVED 2026-07-14**

**Severity:** HIGH (historical)
**Confidence:** HIGH (proven by repeated execution)
**Affected:** kernel/tests/test_seed_product_walkthrough.py (root cause in orders/models.py `_generate_order_number()`)
**Evidence:** 2026-07-14 verification at ce3b30e: failed 1/10 runs **in isolation** (duplicate `ORD-20260714-1003` within a single seed run) and 2 test classes in full regression (distinct colliding keys). The 4-digit random suffix collides randomly among same-day orders created by the seed walkthrough — an in-run birthday-problem collision, NOT an inter-test race as previously recorded.
**Runtime impact (historical):** Full regression exit code 1 intermittently; when `SeedProductWalkthroughDatasetTest.setUpClass` hit the collision, its 10 tests were skipped (e.g., 1662 of 1672 ran on 2026-07-14).
**Resolution:** BG-002 fix — bounded savepoint-wrapped retry in `Order.save()` (5 attempts, DB constraint remains the arbiter) + suffix widened to 6 digits. Regression tests in `orders/tests/test_order_number_generation.py`. Evidence: CHANGE_LEDGER CL-017, TEST_EXECUTION_LOG Run 009.

### FR-014: Missing ServiceSupplier Synchronization at Profile Activation — **RESOLVED 2026-07-18**

**Severity:** HIGH (historical)
**Confidence:** HIGH (root-caused via direct code inspection, confirmed by a live bug report)
**Affected:** `apps/accounts/services/profile_activation_service.py`, `apps/kernel/models/supplier.py`, seed commands
**Evidence:** `ProfileActivationService._activate()` set `profile.status = ProfileStatus.ACTIVE` but never called the supplier bridge — an activated caregiver/organization could have zero `kernel.ServiceSupplier` row, making them invisible to the public directory/home page/discovery ranking (all of which query `ServiceSupplier`, never `CaregiverProfile`/`OrganizationProfile` directly). `ServiceSupplier.linked_entity_id`/`linked_entity_type` also carried only a plain index, not a database uniqueness constraint, so even `get_or_create()`-based creation elsewhere in the codebase could not guarantee exactly one supplier per identity under concurrent writes. `seed_demo_accounts.py`/`seed_demo_people.py` were confirmed to reproduce this exact condition — both create ACTIVE-by-default profiles into the default tenant with zero supplier sync.
**Runtime impact (historical):** "Public directory pages render successfully but show no providers/organizations despite seeded data" — the live bug report that triggered this remediation's investigation.
**Resolution:** Core Profile-ServiceSupplier Invariant Remediation (ADM-029) — `ProfileActivationService` now synchronously guarantees ServiceSupplier existence+ACTIVE status inside the same activation transaction; a new `UniqueConstraint(linked_entity_id, linked_entity_type)` on `ServiceSupplier`; a one-time data migration + idempotent `reconcile_profile_supplier_invariant` management command for existing drift; `seed_demo_accounts`/`seed_demo_people` now sync suppliers for their ACTIVE-by-default profiles. 35 new tests, full regression 2249 -> 2284/2284 green.
**Remediation addendum (independent pre-merge review of PR #18, 2026-07-18):** the first pass left two gaps this same finding covers. (1) `apps.accounts.services.provider_identity.resolve_supplier_for_user()` was left completely unguarded and, reachable through every `provider_portal` view via `_guard()` (not only the one status page it was justified against), granted a fully ACTIVE `ServiceSupplier` to a DRAFT caregiver — reproduced empirically before the fix. Fixed by splitting identity resolution: `resolve_supplier_for_user()` now raises for a non-ACTIVE profile (used by call sites that genuinely need a working supplier — assignments/visits/availability/earnings); a new `resolve_provider_context_for_user()` never creates a supplier and returns `supplier=None` for self-view pages (profile/dashboard/skills/experience/gallery/etc.), which were reworked to render a not-yet-activated state instead of depending on a real supplier. The organization-side equivalent (`OrganizationProfilePresentationService.get_profile_view()`) received the same treatment. (2) The idempotent already-ACTIVE path in `ProfileActivationService._activate()` returned before ever reaching the supplier-sync call, so an already-ACTIVE profile with a missing/drifted supplier was not repairable by re-activating it — only by the separate reconciliation command. Fixed: that path now also invokes the sync helper (no second status transition, no duplicate audit entry). A third non-ACTIVE creation path was found during the required call-graph recheck and fixed: `seed_demo_orders.py`'s unfiltered `CaregiverProfile.objects.first()` had no status filter or guaranteed ordering. 37 additional tests (INV-10 service-layer and view-level navigation tests for both DRAFT and ACTIVE caregivers/organizations, idempotent-path repair tests including a rollback case, a new `seed_demo_orders`/`seed_demo_accounts`/`seed_demo_people` supplier-sync regression module, a reconciliation-command type-anomaly-visibility test, and guardrail AST-detector self-tests) plus a strengthened AST-based `ProfileStatusTransitionSoleWriterTest` (the prior regex missed `.create(status=...)`/`.update(status=...)`/`update_or_create(defaults={...})`). That stricter detector caught one previously-invisible, non-regressive offender in `seed_product_walkthrough.py` (deterministic demo caregivers created a `ServiceSupplier` without ever setting `CaregiverProfile.status` to `ACTIVE`) — this was a real instance of the same defect class, not an offender to allowlist, so `seed_product_walkthrough.py` now explicitly passes `status=ProfileStatus.ACTIVE` to `ensure_caregiver_profile()` for its deterministic providers (mirroring the same explicit-ACTIVE convention `_ensure_organizations()` already used), and no allowlist exception was added for it. Full regression 2284 -> 2321/2321 green, 0 failures, 0 errors, no warnings; both adversarial Scenario A/B re-verifications re-run and passing. A second independent focused review of the remediation returned `APPROVE`, independently re-running the full regression (2321/2321, matching exactly) and 7 fresh adversarial checks. **PR #18 merged to `main`** (merge commit `096d7c50d2b912b52ae60468653cc2ce0df8798a`, final PR head `3d723cbcd1237aa2ba24a151aabc9b9cff80bb20`, 2026-07-18). FR-014 is now merged and closed.

### FR-015: Public Caregiver and Organization Directory Views Failed to Honor Explicit Tenant Hints — **RESOLVED 2026-07-18**

**Severity:** HIGH
**Confidence:** HIGH (both symptoms reproduced against a real running server, root-caused via direct code inspection, each fixed with a failing-then-passing regression test)
**Affected:** `apps/public_site/views.py` (`find_a_caregiver`, `find_an_organization`)
**Evidence:** Both public *detail* views (`caregiver_profile()`, `organization_profile()`) resolve an explicit `?tenant=<slug>` query hint via the single shared `_resolve_optional_tenant_hint()` helper and pass the resolved `tenant_id` into their respective profile services — added specifically so a supplier living in a non-default tenant (e.g. `seed_product_walkthrough`'s own dedicated demo tenant) can be previewed. Neither *directory/listing* view (`find_a_caregiver()`, `find_an_organization()`) adopted the same hint resolution: both called their directory service (`CaregiverDirectoryService.search()` / `OrganizationDirectoryService.search()`) without a `tenant_id` argument at all, so each service silently defaulted to `TenantService.get_default_tenant_id()` regardless of the query string. An entity in any non-default tenant — proven ACTIVE, VERIFIED, ACTIVE `ServiceSupplier`, correct type, publicly visible per `common.is_publicly_visible_attrs()`, confirmed 200 + fully rendered at its own direct detail URL — was therefore invisible in the corresponding directory for that same tenant.

**Caregiver symptom (found and fixed first):** `GET /find-a-caregiver/?tenant=demo-senior-platform` rendered `0 مراقب یافت شد` with no filters applied, while `GET /find-a-caregiver/<id>/?tenant=demo-senior-platform` rendered the same caregiver correctly.

**Organization symptom (latent — masked by an unrelated, separate seed-data limitation):** the same defect exists in `find_an_organization()`, but was invisible in ordinary manual testing because every organization `seed_product_walkthrough` creates has `verification_status=unverified`, which already excludes them from the directory for an unrelated reason (`common.is_publicly_visible_attrs()`'s verification predicate). Proven directly, not assumed: a real ACTIVE, VERIFIED, ACTIVE-supplier organization was constructed in the demo tenant via the canonical `get_or_create_supplier_for_organization()` bridge — `GET /find-an-organization/?tenant=demo-senior-platform` rendered `0 سازمان یافت` for it, while `GET /find-an-organization/<id>/?tenant=demo-senior-platform` rendered it correctly (200, fully rendered) — the identical directory-vs-detail contrast already proven for caregivers. The verification-status limitation is real and remains unfixed (see Remaining Known Risks below) but is a **distinct, unrelated cause** from the tenant-hint gap — fixing the tenant-hint gap does not and must not make unverified organizations publicly visible, and it does not: the constructed test organization was explicitly VERIFIED to isolate and prove the tenant-hint defect specifically.

**Shared root cause:** the tenant-hint resolution helper was wired into both detail views when the multi-tenant preview feature was introduced, but was never symmetrically wired into either directory view — an omission, not a design decision (both directory services already correctly accept and use a `tenant_id` keyword argument, confirmed by their own existing service-level tests, which pass `tenant_id` explicitly; the gap was entirely in the view layer, not the service or query layer). Root-cause proven stage-by-stage for both entity types: querying with `tenant_id=TenantService.get_default_tenant_id()` excluded the known entity at the very first stage (initial candidate population); querying with the correct tenant included it through every subsequent stage.

**Runtime impact:** An entity correctly activated, verified, and supplier-synchronized by the Core Profile-ServiceSupplier Invariant Remediation (FR-014/PR #18) was still invisible in the one surface most visitors actually use — the directory listing — even though its own direct profile URL worked. For organizations specifically: once an organization becomes verified through the normal verification workflow, it would have silently reproduced the same class of failure the caregiver fix already resolved, had this not been proactively fixed here rather than left masked behind the current seed-data limitation.

**Resolution:** `find_a_caregiver()` and `find_an_organization()` both now call `_resolve_optional_tenant_hint(request)` — the same helper both detail views already use — and pass the resolved `tenant_id` into their respective directory service. A two-line change per view, symmetrical, no duplicated tenant-resolution logic (a single shared helper, already defined once in this module, now used identically by all four public views). No service, query, or visibility-rule change in either case; no organization seed-verification behavior changed; no unverified organization was made publicly visible. Callers that never pass `?tenant=` get exactly the pre-existing default-tenant behavior for both entity types; an unknown slug now correctly 404s on both directories instead of silently rendering an empty list, matching both detail views' own established contract.

Regression tests, symmetrical for both entity types (`FindACaregiverViewTenantHintTest` + `FindAnOrganizationViewTenantHintTest`, 8 tests each, mirroring `CaregiverProfileTenantHintTest`/`OrganizationProfileTenantHintTest`): correct hint lists the entity; missing hint still excludes a non-default-tenant entity; unknown slug 404s; wrong tenant hint cannot cross-tenant-leak; unverified/DRAFT/inactive-supplier entities remain excluded even with a correct hint; and a consistency test proving an entity visible via the detail-page hint is also visible via the directory hint for the same unfiltered request. All 16 confirmed failing before their respective fix and passing after. Full regression 2321 -> 2329/2329 green (caregiver fix) -> **2337/2337 green** (organization fix), 0 failures, 0 errors, no warnings at each step. Branch `fix/public-caregiver-directory-visibility`, PR #19 **merged to `main`** (merge commit `aac018575a2f9380d75a11c55bf08b34ce65a511`, 2026-07-18).

### FR-016: Public Directory Views Fixed Tenant-Hint *Resolution* (FR-015), but Directory-Generated Navigation Still Dropped It — **RESOLVED 2026-07-19**

**Severity:** HIGH
**Confidence:** HIGH (reproduced against a real running server, root-caused via direct code inspection, fixed with 6 failing-then-passing regression tests covering cards/pagination/filters/reset for both entity types)
**Affected:** `apps/public_site/services/directory_service.py`, `apps/public_site/services/organization_directory_service.py`, `apps/public_site/services/common.py`, `apps/public_site/services/viewmodels.py`, `apps/public_site/views.py`, `templates/public_site/caregiver_directory.html`, `templates/public_site/organization_directory.html`
**Distinction from FR-015:** FR-015 (PR #19) fixed tenant *resolution* at the two directory **views**' own boundary — `find_a_caregiver()`/`find_an_organization()` now correctly read `?tenant=<slug>` and scope their own query to it. This follow-up fixes tenant-*context propagation* through the **URLs those directories generate** for further navigation (caregiver/organization card links, pagination links, the filter form, the "clear filters" link) — a distinct defect at the template/URL-building layer, not the tenant-resolution layer FR-015 already closed.
**Evidence:** `GET /find-a-caregiver/?tenant=demo-senior-platform` correctly renders 200 with real caregivers (FR-015's fix working as intended). But every rendered caregiver card link (`CaregiverCardViewModel.profile_url`, built in `CaregiverDirectoryService._build_card()`) was a hardcoded `f"/find-a-caregiver/{supplier.id}/"` with no query string at all — clicking any card took the visitor to `GET /find-a-caregiver/<supplier-id>/` (no hint), which resolves the *default* tenant and 404s for a supplier that lives anywhere else. The same gap existed in pagination links (`_build_pagination()`'s `params` dict never included `tenant`), the filter `<form method="get">` (no hidden `tenant` field, so a GET submission drops the query string entirely, keeping only the form's own named inputs), and the "clear filters" empty-state link (hardcoded `action_href="/find-a-caregiver/"`). Identical shape confirmed in `find_an_organization()`/`OrganizationDirectoryService`.
**Runtime impact:** A visitor who reached the directory via a valid, explicit tenant hint (the only way to browse a non-default tenant at all, e.g. a demo/preview link) could see results but could not click into any of them, use pagination, resubmit a filter, or clear filters without the entire browsing session silently falling back to the wrong (default) tenant — a broken, not merely inconvenient, navigation experience for every non-default-tenant visitor.
**Resolution:** Added one shared helper, `common.append_tenant_query(url, tenant_slug)` — returns `url` unchanged when no hint is active, otherwise appends `?tenant=<slug>`; navigation context only, never trusted as authoritative (the destination view still independently re-resolves and re-validates the hint via the same `_resolve_optional_tenant_hint()` FR-015 already established). Both directory services now accept an optional `tenant_slug` parameter (the view passes the already-validated raw slug string alongside the already-resolved `tenant_id` — the slug is never used for query filtering, only for URL construction) and thread it through: `_build_card()` appends it to `profile_url` via the shared helper; `_build_pagination()`'s `params` dict includes `tenant` when present (reusing the pre-existing, unmodified `common.build_pagination()` query-string-preservation mechanism — no new pagination-URL logic); `_build_filters()` exposes the slug as a new `tenant_slug` field plus a computed `reset_url` field on `DirectoryFiltersViewModel`/`OrganizationDirectoryFiltersViewModel`, so both directory templates render a hidden `<input type="hidden" name="tenant" value="...">` inside the filter form (a GET-form hidden field is resubmitted automatically with every filter change) and use `reset_url` for the "clear filters" link instead of a hardcoded path. Zero behavior change for `featured()` (Home Page) and `build_cards_for_supplier_ids()` (Customer Favorites) — neither passes `tenant_slug`, so their card URLs are byte-identical to before.
**Tests:** 8 new (`DirectoryTenantLinkPropagationTest`): caregiver card links carry the hint and resolve (200); caregiver pagination links carry the hint and resolve; caregiver filter form's hidden field carries the hint and a resubmission stays scoped to the same tenant; caregiver "clear filters" link carries the hint and resolves; missing/wrong tenant context still excludes cross-tenant caregivers (no leak); the organization-side equivalents (card links + combined pagination/filter/reset); unverified organizations remain absent regardless of the hint. 6 of 8 confirmed failing against the pre-fix code (verified by temporarily stashing the production diff and running the new tests against the original, unmodified views/services) and all 8 passing after; the other 2 (leak-prevention, unverified-exclusion) correctly passed both before and after, since they assert the absence of something the fix was never meant to change.
**Verification:** `git diff --check` / `manage.py check` / `manage.py migrate --check` all clean. Focused suites (`apps.public_site`, `apps.discovery`, `apps.kernel.tests.test_supplier_registry`, `apps.kernel.tests.test_supplier`, `apps.kernel.tests.test_seed_product_walkthrough`, `apps.accounts.tests.test_profile_supplier_invariant`) — 374/374 pass. Full regression: **2337 -> 2345/2345 green**, 0 failures, 0 errors, no warnings. Manual runtime verification against a real local server (re-seeded via `seed_product_walkthrough`, plus a disposable verified organization created and deleted for the organization-side check, never committed): 3 caregiver card links followed successfully (200 each); search/city/service/cooperation filters all preserved the hint via the hidden field; "clear filters" preserved the hint; an invalid tenant slug still 404s; the organization card link, filter hidden field, and follow-through all confirmed working identically.
**Preserved, unchanged:** tenant isolation (the destination view is always the authority — a manipulated or stale `tenant` query parameter is re-validated on every request, never trusted because it arrived via a link or hidden field), the default-tenant fallback for hint-less requests, unknown-slug 404 behavior, and every public-visibility predicate (ACTIVE profile, VERIFIED, ACTIVE supplier, correct type, active membership for affiliated caregivers) — none of `CaregiverDirectoryService._filter_candidates()`/`OrganizationDirectoryService._filter_candidates()`/`common.is_publicly_visible_attrs()` was touched.
**Explicitly out of scope, unchanged:** `seed_product_walkthrough`'s organization `verification_status` (still unverified by construction — the same pre-existing, separate limitation FR-015 already documented); no new tenant field or duplicated tenancy data; no demo tenant made globally default; no JavaScript-only navigation introduced (the fix is entirely server-rendered GET links/forms, matching the rest of this codebase's public pages).
**Branch:** `fix/public-directory-tenant-link-propagation`, from `main` @ `aac018575a2f9380d75a11c55bf08b34ce65a511`.
**PR #20 final review addendum (2026-07-19):** Independent pre-merge review of PR #20 found one genuine latent defect in `common.append_tenant_query()`: it unconditionally appended `?tenant=<slug>` to `url` without checking for an existing query string, which would have produced a malformed double-`?` URL (or a duplicated `tenant` param) had any caller ever passed a URL that already carried query parameters. All 4 call sites in this PR happened to always pass a bare path, so no shipped navigation surface was actually broken — but the helper's own contract ("the one shared convention every directory-generated link... uses") implies future reuse, so this was fixed during review rather than left latent: the helper now parses any existing query string, merges `tenant` into it, and re-encodes once. 5 new unit tests added (`AppendTenantQueryHelperTest`) proving the merge, encoding, and duplicate-prevention behavior directly. Full regression re-run after the fix: **2345 -> 2350/2350 green**. Review returned `APPROVE`; **PR #20 merged to `main`** (merge commit `e1f0bfd6c921636bab0c196eaf469d2eb2a667aa`, 2026-07-19).

### FR-017: Public Site Had No Canonical Tenant — Homepage Never Resolved Any Tenant Context, and No Deployment Could Configure a Non-Default Public Tenant — **RESOLVED 2026-07-19**

**Severity:** HIGH
**Confidence:** HIGH (reproduced against a real running server, root-caused via direct code inspection and a live data query, fixed with 11 failing-then-passing regression tests)
**Affected:** `apps/public_site/views.py`, `apps/public_site/services/tenant_context.py` (new), `apps/public_site/context_processors.py` (new), `apps/public_site/services/home_service.py`, `apps/public_site/services/directory_service.py`, `apps/public_site/services/viewmodels.py`, `config/settings/base.py`, `templates/public_site/base_public.html`, `templates/public_site/home.html`
**Distinction from FR-015/FR-016:** FR-015 (PR #19) fixed tenant *resolution* in the two directory views for an explicit `?tenant=` hint. FR-016 (PR #20) fixed tenant-*context propagation* through the URLs those directories generate. Both left two gaps this closes: (1) `home()` never called any tenant-resolution helper at all — not even the explicit-hint one FR-015 added to the other four views — so the homepage and its "featured caregivers" section were unconditionally pinned to the platform default tenant, hint or no hint; (2) there was no mechanism, anywhere, for a deployment to designate a *different*, already-populated tenant as its public site — the only way to reach any non-default tenant's data was for every single visitor to manually append `?tenant=<slug>` to every single URL, which is not a usable product experience for a non-technical visitor.
**Evidence:** `GET /find-a-caregiver/` (the bare root an ordinary visitor actually types) rendered `0 مراقب یافت شد`, while `GET /find-a-caregiver/?tenant=demo-senior-platform` rendered 10 real caregivers. Root-caused to two independent, compounding facts, both confirmed directly against the running database rather than assumed: (a) the platform default tenant (`salmandyar`, `TenantService.DEFAULT_TENANT_SLUG`) does have 4 ACTIVE `ServiceSupplier` rows (seeded by `seed_demo_accounts`), but all 4 have `verification_status="unverified"` — `common.is_publicly_visible_attrs()` correctly excludes them, exactly as it should (a legitimate, intentional data-quality gate, not a bug — an unverified caregiver must not be publicly listed); (b) `seed_product_walkthrough`'s dedicated `demo-senior-platform` tenant *does* have real, VERIFIED, ACTIVE caregivers, but nothing in the codebase told the public site to look there instead of the platform default — `apps.kernel.models.tenant.Tenant.domain` exists on the model but is read by zero lines of application code anywhere in the repository (grepped and confirmed), and no `django.contrib.sites`/`SITE_ID`/host-based resolver of any kind existed.
**Runtime impact:** A non-technical visitor opening `http://127.0.0.1:8000/` in this local development environment — or any deployment whose intended public catalog happens to live in a non-default tenant — saw an apparently broken, empty product with no caregivers or organizations anywhere, and had no way to reach the real data without being told the exact tenant slug to type into the address bar by hand.
**Resolution:** New canonical resolver `apps.public_site.services.tenant_context.resolve_public_tenant(request)` — see that module's own docstring for the full contract — used by all five public views (`home`, `find_a_caregiver`, `caregiver_profile`, `find_an_organization`, `organization_profile`), replacing the old per-view, homepage-excluding `_resolve_optional_tenant_hint()`. Resolution order: (1) an explicit `?tenant=<slug>` hint — unchanged FR-015 behavior, 404s on an unknown slug; (2) `settings.PUBLIC_SITE_TENANT_SLUG`, a new, optional, environment-variable-driven setting (see `.env.example`) — unset by default in every shipped settings module, so every existing deployment and test is behavior-identical to before; (3) the platform default tenant, unchanged. A misconfigured (2) — set to a slug that resolves to no real tenant — raises `django.core.exceptions.ImproperlyConfigured` rather than silently falling back, so a deployer's typo is loud, not a silently-wrong public site. `HomePageService.get_home_view()` now accepts `tenant_id`/`tenant_slug`, threading the latter into `CaregiverDirectoryService.featured()` (extended with a new optional `tenant_slug` parameter, default `None`, zero behavior change for any pre-existing caller) and into `HomePageViewModel`'s new `tenant_slug`/`caregiver_directory_url` fields, all built via the existing, review-hardened `common.append_tenant_query()` — no new URL-building logic. A new context processor, `apps.public_site.context_processors.public_tenant_context` (registered in `TEMPLATES`, scoped internally to `request.resolver_match.namespace == "public_site"` so it costs nothing on any other app's page), exposes `public_tenant_slug` to every public template, so `base_public.html`'s shared desktop nav, mobile nav, and footer links (6 occurrences total) carry the resolved tenant forward on every public page, not only the ones that already resolved it themselves.
**Tests:** 11 new failing-then-passing (`PublicSiteCanonicalTenantResolutionTest`, `apps/public_site/tests/test_canonical_tenant_resolution.py`, 15 total including 4 that correctly pass both before and after since they assert pre-existing behavior — explicit-hint priority, unknown-hint 404, isolation, unset-setting no-op): homepage/directory/detail pages use the configured tenant with zero hint; homepage and directory-page nav links carry it; a featured-caregiver card link resolves without a manual hint; the homepage search form's hidden field carries it; pagination and reset links carry it; an explicit hint still overrides the configured tenant; an unknown explicit hint still 404s even with a tenant configured; a misconfigured setting raises `ImproperlyConfigured` rather than falling back; leaving the setting unset is unchanged; the configured-tenant path still excludes a third tenant's data (isolation holds). 11 of 15 confirmed failing against the pre-fix code (verified by temporarily stashing the production diff and running the new tests against the original, unmodified views/services) and all 15 passing after.
**Verification:** `git diff --check` / `manage.py check` / `manage.py migrate --check` all clean (no model changes). Investigated per task instruction: `manage.py makemigrations --check --dry-run` shows pre-existing drift confined entirely to `apps.kernel` (roughly 30 "Alter field" entries — `help_text`/`verbose_name`/`choices` metadata only, no add/remove/rename, nothing `migrate --check` flags as pending schema). Unrelated to `apps.public_site` and to this fix (zero model changes in this PR) — not modified, and no migration generated, per explicit instruction not to touch it unless proven relevant to the current model state, which it is not. Focused `apps.public_site` — 255/255 pass. Full regression: **2350 -> 2365/2365 green**, 0 failures, 0 errors, no warnings. Manual runtime verification against a real local server with `PUBLIC_SITE_TENANT_SLUG=demo-senior-platform` set via a local, gitignored `.env` (never committed): `GET /` returned 200 and rendered 4 real featured-caregiver cards, all carrying `?tenant=demo-senior-platform`; 3 of those card links followed successfully (200 each); `GET /find-a-caregiver/` (bare, no hint) rendered `10 مراقب یافت شد`; the "پیدا کردن سازمان" nav link on that same page carried the same tenant hint; the homepage's filter form hidden field and the empty-state reset link both carried it; an unknown `?tenant=` slug still 404d; a valid explicit `?tenant=demo-senior-platform` override still worked; the platform default tenant's own data (`مدیر سازمان نمونه`) did not leak into the demo-configured homepage.
**Preserved, unchanged:** tenant isolation (the destination view is always the sole authority — the configured/hinted slug is used only to select which validated `tenant_id` to query with, never to weaken which rows that `tenant_id` returns); the default-tenant fallback and unknown-slug 404 behavior for every deployment that leaves the new setting unset; every public-visibility predicate (`common.is_publicly_visible_attrs()` untouched); `featured()`'s and `build_cards_for_supplier_ids()`'s (Customer Favorites) byte-identical output for any caller that doesn't pass `tenant_slug`.
**Explicitly out of scope, unchanged:** `demo-senior-platform` is never hard-coded anywhere in source — `PUBLIC_SITE_TENANT_SLUG` is an environment-variable-driven setting, unset in every committed settings module, set only via this local environment's own gitignored `.env` file; `seed_product_walkthrough`'s organization `verification_status` (still unverified by construction, the same pre-existing FR-015/FR-016 limitation — the organization directory still correctly shows 0 results under the demo tenant for that unrelated, already-documented reason); the pre-existing kernel migration-drift warning was not investigated further or "fixed" as part of this task, per explicit instruction — it remains a separate, unrelated, already-tracked item.
**Branch:** `fix/public-site-canonical-tenant-resolution`, from `main` @ `e1f0bfd6c921636bab0c196eaf469d2eb2a667aa`. An independent final pre-merge review returned `APPROVE`; **PR #21 merged to `main`** (merge commit `5e4364c868ea4082f40bd4588089951a8c81f247`, 2026-07-19).

### FR-018: Public Site Coherence Remediation (PSA-001 through PSA-005) — **RESOLVED 2026-07-19**

**Severity:** MEDIUM (HIGH for PSA-003's workflow-conversion gap; the remaining findings are UX/branding/professionalism, not functional or security failures)
**Confidence:** HIGH (every finding reproduced against a real running server before the fix, root-caused via direct code inspection, fixed with failing-then-passing regression tests)
**Affected:** `ui/layouts/base.html`, `ui/components/public/seo_meta.html`, `apps/public_site/templatetags/public_site_tags.py` (new), `apps/public_site/views.py`, `apps/public_site/services/viewmodels.py`, `apps/public_site/services/organization_directory_service.py`, `templates/public_site/base_public.html`, `templates/public_site/organization_directory.html`, `templates/public_site/caregiver_profile.html`, `templates/public_site/contact.html`, `apps/kernel/views.py` (new), `templates/errors/403.html` (new), `config/urls.py`, `static/favicon.svg` (new)
**Origin:** a code-and-runtime-backed audit of the whole public site as an ordinary end user (not a numbered sprint), performed after PR #21 (FR-017) merged — see the audit's own report for the full finding list, severity classification, and evidence. This entry documents only the 5 material findings selected for remediation (PSA-001–005) plus the low-risk polish items bundled with them; the audit's remaining LOW/OBSERVATION findings (no breadcrumb on caregiver profiles — now fixed as part of PSA-003; the dev-only verbose 403 debug page under `DEBUG=True`, which is correctly absent in `config.settings.production`; untested live pagination against real demo data) are left as recorded observations, not separately remediated here.

**PSA-001 (page-title branding):** `ui/layouts/base.html`'s `title_suffix` block was hard-coded to the generic word "پلتفرم" ("Platform") instead of the brand "سالمندیار" — every public page's browser-tab title read "‹page› | پلتفرم". Separately, `ui/components/public/seo_meta.html` appended a *second* " | سالمندیار" on top of every caller's own `page_title` argument, which every one of the ~20 public templates already embeds the brand into — doubling the brand in `og:title`/`twitter:title` on every single public page, not only the homepage the audit specifically evidenced. **Fix:** corrected the one hard-coded suffix string; removed `seo_meta.html`'s own redundant append (single canonical composition point, per-page `<title>` still fully overridable via the existing `{% block public_title %}` chain).

**PSA-002 (missing favicon):** no `<link rel="icon">` anywhere and `/favicon.ico` 404'd — every tab showed a broken/default icon. **Fix:** added a small project-owned SVG favicon (`static/favicon.svg` — a rounded square in the platform's existing primary color with the "س" initial already used as the nav brand mark; explicitly provisional, not a final-branding approval) and one `<link rel="icon" type="image/svg+xml" href="/static/favicon.svg">` in the one shared `<head>` template. No external CDN dependency, no generated build artifact committed.

**PSA-003 (caregiver-profile CTA lost caregiver context):** the caregiver profile's only conversion action, "شروع گفتگو" ("Start conversation"), linked to a generic, caregiver-agnostic `/contact/` with no reference to which caregiver was being viewed. **Product interpretation:** this PR does **not** introduce direct booking, contracting, messaging, payment, or assignment — the existing `/contact/` page is itself still a non-functional demo form (`method="post" action="#"`, submit button `type="button"`, its own on-page copy already discloses "این فرم هنوز به backend ارسال پیام متصل نشده است") with no `apps.orders`/order-placement wiring, and none was added. The remediation is scoped exactly to what PSA-003 asked for: preserve the visitor's caregiver selection across the navigation. **Fix:** the CTA now reads "درخواست مشاوره درباره این مراقب" ("Request a consultation about this caregiver") and links to `/contact/?caregiver=<supplier_id>&tenant=<slug>` — the id is the same server-validated identifier `caregiver_profile()` itself already resolved (never a client-supplied name). `contact()` re-validates it independently through the unchanged `CaregiverPublicProfileService.get_profile()` (same tenant resolution via `resolve_public_tenant()`, same `is_publicly_visible_attrs()` gate every other public surface uses) — a malformed, unknown, or foreign-tenant id 404s rather than being trusted or silently ignored. On success, `/contact/` greets the visitor with the caregiver's own `display_name` (auto-escaped, never the raw id rendered as visible text). Ordinary `/contact/` access with no `?caregiver=` hint is unaffected. The view performs no write of any kind — opening the page creates no order/contract/assignment row (there is nothing in this codebase's `/contact/` path that could, since it was never wired to one). Also added: a "بازگشت به فهرست مراقبان" ("back to the caregiver list") link on the profile page, carrying the same tenant context, closing the audit's separately-noted missing-breadcrumb observation.

**PSA-004 (misleading unfiltered organization empty state):** `/find-an-organization/` with zero filters applied showed "سازمانی با این فیلترها یافت نشد" ("no organization found *with these filters*") even though none were set — misleading given the real cause (no verified organizations exist yet in this tenant, a pre-existing FR-015/FR-016 seed-verification limitation, unrelated to and unchanged by this fix). **Fix:** `OrganizationDirectoryFiltersViewModel` gained `has_active_filters`, computed in `_build_filters()` from *normalized, validated* inputs only — non-empty trimmed search text, a city string that survives whitespace/casing normalization, or a service-category id that matches a real category the tenant actually has — never from raw query-string presence, so a garbage/unrecognized `?service=` value does not falsely trigger the filtered-state copy. The template now renders one of two distinct messages: "در حال حاضر سازمان تأییدشده‌ای برای نمایش وجود ندارد." (no filters, no verified organizations) or the original filtered-state copy with a "پاک کردن فیلترها" reset button, shown only when a filter is genuinely active. Tenant isolation and the verification gate are unchanged — this is a display-logic fix only, not a data-visibility change; a real, verified organization still renders normally.

**PSA-005 (raw Django 403 for anonymous users):** `/portal/`, `/provider/`, `/organization/`, `/admin-portal/` all showed Django's unbranded default 403 page. The task's own first step ("first inspect the intended access-control architecture") mattered here: an initial implementation that redirected anonymous GET requests to login broke 15 pre-existing tests across `apps.portal`, `apps.provider_portal`, `apps.organization_portal`, and `apps.admin_portal` (`UnauthenticatedAccessTest.test_every_page_denies_anonymous_users` and several per-view siblings), which explicitly and redundantly assert anonymous access returns a plain 403 — never a redirect — for every one of these four apps. That is this codebase's own established, deliberately non-disclosing security policy (an anonymous prober should not learn "this route exists and requires login" via a redirect), exactly the exception PSA-005 itself named up front. **Fix, respecting that discovered policy:** a new `handler403` (`apps.kernel.views.forbidden`, registered in `config/urls.py`) renders one branded, Persian, non-disclosing `templates/errors/403.html` page for every browser-facing `PermissionDenied` — anonymous or authenticated-wrong-role alike, status code unchanged (still 403) — instead of Django's raw default. `/api/` requests are explicitly left untouched (`apps.api` already raises its own `ApiError` → JSON envelope before this handler is ever reached; this is a defense-in-depth fallback, not the primary API error path). No permission-check function anywhere was modified, no status code changed, and none of the 15 pre-existing tests needed to change. A `_safe_next_path()` open-redirect guard (Django's own `url_has_allowed_host_and_scheme()`) was still written and unit-tested, kept available for a future login-redirect flow should this project's access-control policy ever change, but is not currently wired into the live handler.

**Open Graph / metadata cleanup:** `og:url` is now absolute (`request.build_absolute_uri()` via a new `absolute_url` template filter — never a hard-coded host) and agrees with `<link rel="canonical">` (both derive from the same `page_url`/`canonical_url` argument through the same filter); `og:title`/`twitter:title` no longer duplicate the brand (see PSA-001's own fix — the same root cause).

**Other low-risk polish:** removed the internal build-process HTML comment ("remove after `npm run build` regenerates output.css") that shipped in every page's `<head>`. The 3 footer social icons (`href="#"`) are now non-interactive `<span>`s (no false affordance of being clickable) rather than dead links — no real social account exists yet to link to, and none was invented.

**Tests:** 39 new (`apps/public_site/tests/test_page_metadata.py`, `test_caregiver_contact_context.py`, `test_organization_empty_state.py`, `apps/kernel/tests/test_http_error_handlers.py`) — PSA-001 title composition, PSA-002 favicon, OG/canonical-URL cleanup, no `href="#"` placeholders, PSA-003 caregiver-context CTA/contact validation, PSA-004 empty-state distinction, PSA-005 branded-403 behavior.
**Verification:** `git diff --check` / `manage.py check` / `manage.py migrate --check` all clean (no model changes, kernel migration drift not touched). Focused suites: `apps.public_site`, `apps.kernel`, `apps.portal`, `apps.provider_portal`, `apps.organization_portal`, `apps.admin_portal`, `apps.accounts`, `apps.api` — 1470/1470 pass (confirms the PSA-005 redesign fixed all 15 initially-broken pre-existing tests, with zero test-file edits to any of them). Full regression: **2365 -> 2404/2404 green**, 0 failures, 0 errors, no warnings (run with the local `.env` temporarily set aside to avoid `PUBLIC_SITE_TENANT_SLUG` contaminating the empty test database, restored immediately after). Manual runtime verification against a real local server (`.env` restored, `PUBLIC_SITE_TENANT_SLUG=demo-senior-platform`): `/` title reads "خانه | سالمندیار"; favicon link present and resolves 200; `og:url`/canonical both absolute and identical; zero `href="#"` anchors; `/find-a-caregiver/` and `/find-an-organization/` both 200; 3 caregiver profiles followed successfully with the CTA carrying `?caregiver=<id>&tenant=<slug>` and the back-link present; `/contact/?caregiver=<id>&tenant=<slug>` correctly greeted the visitor by name, a malformed/unknown caregiver id both 404d, and hint-less `/contact/` still worked unchanged; the bare organization directory showed the no-verified-organizations message with no reset button while a filtered, zero-match query showed the filtered message with a reset button; anonymous `/portal/`/`/admin-portal/` both returned a branded 403; no cross-tenant leak; every footer/nav route still 200.
**Preserved, unchanged:** every permission-check function's actual authorization decision; tenant isolation and the public-visibility gate (`is_publicly_visible_attrs()` untouched); the default-tenant/unset-setting fallback behavior from FR-017; `apps.api`'s own 401/403 JSON error contract; the established 403-not-redirect policy across all four portals (now confirmed, not merely preserved by accident).
**Explicitly out of scope:** no direct caregiver booking, messaging, payment, or order-placement workflow introduced (PSA-003 stays a context-preservation fix, not a new feature); no seed or verification data changed (the organization-directory zero-result *data* state is unchanged — only the message describing it); tenant resolution precedence (FR-017) untouched; kernel migration drift not investigated further, per explicit instruction.
**Branch:** `fix/public-site-coherence-remediation`, from `main` @ `e9fede1620d4b19ecc294f08cd86591997c4cacf`. An independent focused review of PSA-003 (caregiver-context flow) returned no correctness, security, or tenant-isolation blockers; **PR #22 merged to `main`** (merge commit `394b0a71688a333143084d180a59d1ae5a551957`, 2026-07-19). Post-merge full regression re-verified at 2404/2404.

### FR-019: Public Caregiver Marketplace — Zero-Result Directory Root Cause, Card-Link/Avatar Gaps, and Barren Seed Profiles — **RESOLVED 2026-07-19** (corrected same day after independent review)

**Severity:** MEDIUM (once corrected, the canonical `/find-a-caregiver/` URL requires zero manual configuration; the two real code gaps found alongside it, and the seed command's own missing showcase data, remain UX/demo-quality issues, not security or correctness failures)
**Confidence:** HIGH (root cause proven by direct reproduction; the corrected canonical-tenant-contract fix proven end-to-end against a completely clean process environment — no `.env` file present at all, and separately with a fresh `.env.example` copy with only the sandbox's own non-standard `DATABASE_PORT` adjusted — not merely asserted)
**Affected:** `apps/kernel/dev_tenant.py` (new), `apps/public_site/services/tenant_context.py`, `apps/public_site/services/common.py`, `apps/public_site/services/directory_service.py`, `apps/public_site/services/profile_service.py`, `apps/public_site/services/viewmodels.py`, `apps/kernel/management/commands/seed_product_walkthrough.py`, `apps/kernel/tests/test_seed_product_walkthrough.py`, `apps/public_site/tests/test_canonical_tenant_resolution.py`, `.env.example`, `templates/public_site/caregiver_profile.html`, `ui/components/public/caregiver_card.html`, `apps/public_site/tests/test_caregiver_discovery_remediation.py` (new), `apps/public_site/tests/test_gallery_public.py`
**Origin:** a report that `CaregiverProfile.objects.count()` showed a healthy total (real caregivers seeded, real `ServiceSupplier` rows synced) while `/find-a-caregiver/` rendered "0 caregivers found."

**Root cause (not a filtering/visibility defect):** every predicate in the directory's eligibility pipeline (`ServiceSupplier.status=ACTIVE`, `supplier_type` in the two caregiver types, `common.is_publicly_visible_attrs()` — profile `status=active`, `verification_status=verified`, owning account `is_active`, and for organization-affiliated caregivers an active `OrganizationMembership`) was individually re-verified against the real seeded dataset and found correct: of 11 caregiver-type suppliers seeded by `seed_product_walkthrough` under its own dedicated `demo-senior-platform` tenant, exactly 10 are publicly visible — the 11th is a deliberately-seeded suspended-membership example, correctly excluded by design (see that command's own `_ensure_affiliated_providers()` comment). Query-string handling was also re-verified correct: `views.py`'s `find_a_caregiver()` already normalizes `city`/`service`/`availability` with `... or None`, so `?city=`/`?availability=`/`?service=` (present but empty) behave identically to the parameter being absent. The real cause is tenant resolution: `resolve_public_tenant()`'s no-hint fallback resolves the platform's single hardcoded default tenant (`TenantService`'s `salmandyar`), never the dedicated `demo-senior-platform` tenant `seed_product_walkthrough` deliberately seeds its realistic dataset into (by design — the tenant boundary is that command's own `--reset-demo` safety mechanism).

**Corrective review (2026-07-19, same day):** the first pass of this fix stopped at documentation — the seed command's report was made to print the real directory URL and explain the `PUBLIC_SITE_TENANT_SLUG` dependency, but the canonical public URL itself still required a manual `.env` edit to work with zero query parameters. An independent review rejected this as insufficient: "That is operational documentation, not a product-level resolution." Corrected by establishing an actual, code-level canonical-tenant contract between the seed and the resolver — see "Canonical tenant contract" immediately below — rather than relying on an operator reading the seed's printed output.

**Canonical tenant contract (the actual fix):** a new `apps.kernel.dev_tenant` module defines exactly one literal, `CANONICAL_DEV_TENANT_SLUG = "demo-senior-platform"` — a plain, Django-app-independent constant with zero model/ORM imports. `seed_product_walkthrough.py`'s own `DEMO_TENANT_SLUG` is now `= CANONICAL_DEV_TENANT_SLUG` (imported, not redefined), and `resolve_public_tenant()` gained a new case 3 — inserted between the existing case 2 (`settings.PUBLIC_SITE_TENANT_SLUG`) and case 4 (renumbered from 3, the platform default): a `settings.DEBUG`-only, best-effort lookup of that exact slug. If a tenant with that slug exists (i.e. `seed_product_walkthrough` has been run), the public site resolves to it with zero `?tenant=` hint and zero `PUBLIC_SITE_TENANT_SLUG` configuration; if it doesn't exist yet (every test database, and any deployment that hasn't run the walkthrough seed), the lookup returns `None` and silently falls through to the unchanged platform-default behavior — it never raises, never invents a tenant, and never picks "the first active tenant." The branch is structurally unreachable outside local development: `config.settings.production` and `config.settings.testing` both hardcode `DEBUG = False` (verified by reading their actual source text, not merely assumed — see `ProductionSettingsNeverAutoResolveDevTenantTest`), the identical guard mechanism `seed_product_walkthrough` itself already uses ("refuses to run when DEBUG is False"). Case 1 (explicit `?tenant=`) and case 2 (`PUBLIC_SITE_TENANT_SLUG`) both still take priority over case 3 unchanged — a deployer's or visitor's explicit choice is never silently overridden by the auto-detected dev tenant. `config/settings/base.py`'s own comment ("never a literal default here... for every deployment to inherit") remains true and unviolated: the literal slug lives in one small, dependency-free module, never in `base.py`, and is only ever consulted when `DEBUG=True`.

**Real defect 1 — directory cards built via string concatenation, not Django's named route:** `CaregiverDirectoryService._build_card()` built each card's `profile_url` as `f"/find-a-caregiver/{supplier.id}/"` instead of `reverse("public_site:caregiver-profile", args=[supplier.id])`, even though that named URL already exists and is used everywhere else a caregiver profile is linked. **Fix:** now uses `reverse(...)`, verified byte-for-byte identical to the route Django itself generates (see `DirectoryCardUsesNamedUrlTest`). No URL shape actually changed; this closes a latent maintenance hazard (a future URL-pattern change would silently desync the two).

**Real defect 2 — a caregiver's own uploaded avatar never reached any public page:** `CaregiverProfile.avatar` (settable via the existing `ProfileMediaService.set_caregiver_avatar()`, the same owner-authorized path the provider portal already uses) was resolved nowhere in `common.bulk_supplier_attrs()`, so every public caregiver card and profile page showed initials-only, even for a caregiver who had uploaded a real photo — unlike the organization side's own `logo_url`, added in Sprint 3.2 but never mirrored for caregivers. **Fix:** `bulk_supplier_attrs()` now resolves `avatar_url` (empty string when unset) from the already-fetched entity — zero additional queries — surfaced through both `CaregiverCardViewModel.avatar_url` and `CaregiverProfileViewModel.avatar_url`, rendered via the existing `ui/components/data/avatar.html`'s pre-existing `src=` prop (already built for exactly this, never previously wired up on the caregiver side). No new component, no new query.

**Seed report improvement (retained, no longer the primary fix):** `seed_product_walkthrough`'s printed report still prints both public directory URLs (tenant-hinted, directly usable as printed) — useful for an operator who wants to preview a *different* configured tenant — but the canonical `/find-a-caregiver/` URL no longer depends on reading or acting on this output; it works from the canonical tenant contract above regardless.

**Seed data gap (Phase 5):** the walkthrough seed created zero `CaregiverSkill`/`CaregiverExperience`/`CaregiverGalleryItem` rows and never set an avatar for any of its 11 demo caregivers — every one of their public profiles silently rendered the skills/experience/gallery sections as empty even though the public profile page has fully supported them since Phase 2.1/Sprint 2.2/2.3. **Fix:** `seed_product_walkthrough` now gives every demo caregiver an avatar (via `ProfileMediaService.set_caregiver_avatar()`), 3 specialty-appropriate skills (via `CaregiverSkillService.add_skill()`), and one experience entry (via `CaregiverExperienceService.create()`); 4 caregivers additionally get 3 approved gallery items each (via `CaregiverGalleryService.add_item()`). All images are small, solid-color, in-memory-generated placeholders (Pillow, the same dependency `apps.accounts.services.image_validation` and this repo's own test suite already use to build test images) — never fetched over the network, never a committed binary asset, never a real or scraped photograph; captions explicitly say "نمایشی" ("demonstration/placeholder"), never describing a real person. Every write goes through the same owner-authorized service every provider-portal caller already uses — no direct model writes. Idempotency is enforced explicitly per helper (skills via the service's own unique-name guard; experience and gallery via an existence check this command adds itself, since neither service has a uniqueness backstop of its own; avatar via a "skip if already set" guard, since `set_caregiver_avatar()` always writes a new file and would otherwise orphan one on every re-run) — verified by running the command three times in a row with zero row-count or file-path drift.

**Gallery presentation (Phase 4):** the public profile's gallery grid became a responsive 1/2/3-column layout (was a fixed 2/3-column grid) with a lightweight, Alpine.js-only lightbox (no new dependency) — each thumbnail is a real `<button>` with an `aria-label` naming the caregiver's own caption, opens a `role="dialog"`/`aria-modal="true"` overlay closable by click-outside, an explicit close button, or Escape. **Caught during runtime verification, not left in:** the first implementation positioned the close button with `-top-3 -end-3` and set the backdrop with `bg-black/80`, `max-h-[85vh]`, and `object-contain` — Tailwind utility classes that were not present in the project's *compiled* `static/css/output.css` bundle (only classes actually scanned out of templates at the last `npm run build` are compiled in, and none of these had been used anywhere else in the codebase before), so they silently had no effect: the close button rendered in normal document flow instead of over the image, and the backdrop never dimmed. Fixed by running `npm install && npm run css:build` (a clean, minimal, purely-additive rebuild — verified byte-identical apart from the new rules, no tool-version banner/timestamp noise) and switching the close button to `top-4`/`end-4` (positive, already-compiled logical-inset utilities, confirmed present both before and after the rebuild) for a small margin of defense against any future un-rebuilt deployment. Re-verified live via Playwright afterward: backdrop `rgba(0,0,0,0.8)`, close button correctly inset at the image's inline-end corner, click-to-close and Escape-to-close both functional.

**Tests:** 46 new/updated. `apps/public_site/tests/test_canonical_tenant_resolution.py` — new `DebugOnlyCanonicalDevTenantAutoResolutionTest` (bare-URL zero-config resolution for both the directory and the homepage, card links resolve, case 3 inactive when `DEBUG=False`, case 1 and case 2 both still override case 3, an unknown hint still 404s even with case 3 available, cross-tenant isolation holds, silent fall-through when no dev tenant has been seeded) and `ProductionSettingsNeverAutoResolveDevTenantTest` (reads `config.settings.production`/`config.settings.testing` source text directly — never imports/executes them, since `production.py` requires a real `SECRET_KEY` env var this test environment doesn't always have — to prove `DEBUG = False` is a hardcoded literal, never environment-driven). `apps/kernel/tests/test_seed_product_walkthrough.py` — new `SeedProductWalkthroughCanonicalTenantAlignmentTest` (the seeded tenant's slug matches `CANONICAL_DEV_TENANT_SLUG` exactly; an end-to-end test that seeds then hits the real `/find-a-caregiver/` URL with zero configuration and asserts a real seeded caregiver's name appears; the same for a caregiver profile URL; a second seed run keeps exactly one canonical-slug tenant; a caregiver seeded under an unrelated tenant is still excluded). Plus the 30 tests from the same-day original pass (directory/gallery/avatar/seed-showcase coverage, unchanged).
**Verification:** `manage.py check` clean. Focused (`apps.public_site.tests.test_canonical_tenant_resolution apps.kernel.tests.test_seed_product_walkthrough`): 91/91. Broader (`apps.public_site apps.kernel`): 588/588. Full regression: **2434 -> 2450/2450 green**, 0 failures, 0 errors (`.env` isolated per policy for the run, restored immediately after). **Fresh-environment runtime evidence (the corrective review's own required proof):** (1) with `.env` renamed away entirely — no file present at all, not merely edited — a brand-new server process started with `env -i` (a fully empty process environment, the closest Linux equivalent to a new PowerShell session) still resolves `GET /find-a-caregiver/` with zero query string to 200 and 10 real caregivers; (2) separately, with `.env` freshly copied from `.env.example` verbatim (only this sandbox's own non-standard `DATABASE_PORT=5433` adjusted to match its actual Postgres port — `PUBLIC_SITE_TENANT_SLUG` left exactly as shipped, commented out) — identical result, 200/10 caregivers, verified via Playwright/Chromium at 375px/768px/1280px: zero horizontal overflow, zero console errors, zero failed or 4xx/5xx requests, gallery lightbox opens/closes correctly, cross-tenant hints (`?tenant=salmandyar`, `?tenant=no-such-slug`) still 404 correctly.
**Preserved, unchanged:** `resolve_public_tenant()`'s case 1/case 2 priority order and every tenant-isolation guarantee (FR-017); `is_publicly_visible_attrs()`'s eligibility rule; the suspended-membership demo caregiver's deliberate exclusion; `config/settings/base.py`'s own "never a literal default here" principle (the literal slug lives in one small, dependency-free module consulted only under `DEBUG=True`, never in `base.py` itself); production and testing settings' `DEBUG = False` (unchanged, hardcoded, verified from source).
**Explicitly out of scope:** no changes to the payment engine, order workflow, authentication, or organization-directory redesign; no verification-policy weakening (no caregiver was made visible merely to populate the page); tenant-isolation principles untouched; kernel migration drift not investigated further.
**Branch:** `fix/public-caregiver-marketplace-remediation`, from `main` @ `0d1d155467173418222d62eb4024c1c24668ae31`, pushed to `origin`. No PR opened and no merge performed — per this task's own scope (implementation, testing, runtime verification, and documentation only; branch/PR/merge were not requested).

### FR-006: UserAccount Queries Not Tenant-Scoped

**Severity:** MEDIUM
**Confidence:** HIGH
**Affected:** accounts/views.py
**Evidence:** Lines 86, 124, 163, 241 — `UserAccount.objects.filter(phone=phone)` without tenant filter.
**Runtime impact:** Phone numbers are globally unique login identifiers. Cross-tenant coupling at auth layer.
**Why it matters:** Design decision, not bug. But means true multi-tenant auth isolation doesn't exist.

---

## MEDIUM Findings

### FR-007: SupplierRegistry Unscoped Queries

**Severity:** MEDIUM
**Confidence:** HIGH
**Affected:** kernel/services/supplier_registry.py
**Evidence:** `find_by_linked_entity()` at line 69-74 has no tenant filter.
**Runtime impact:** Caller must validate tenant independently.

### FR-008: No @login_required Anywhere

**Severity:** MEDIUM
**Confidence:** HIGH
**Affected:** All portal views
**Evidence:** Custom `require_authenticated()` in each portal module instead of Django's `@login_required`.
**Runtime impact:** No single enforcement point to audit. Each module maintains its own auth check.

### FR-009: TenantAwareModel.tenant_id Not a ForeignKey

**Severity:** MEDIUM
**Confidence:** HIGH
**Affected:** All business models
**Evidence:** `common/models.py:53` — `tenant_id = UUIDField(db_index=True)`, not FK to Tenant.
**Runtime impact:** No DB-level referential integrity. No CASCADE/PROTECT on tenant deletion.

### FR-010: Legacy Wallet Still in finance App

**Severity:** MEDIUM
**Confidence:** HIGH
**Affected:** finance/models/wallet.py
**Evidence:** `WalletAccount` and `WalletTransaction` exist in finance but are superseded by apps.wallet.
**Runtime impact:** Confusion for new developers. Potential for wrong-app usage.

---

## LOW Findings

### FR-011: common App Has Zero Tests

**Severity:** LOW
**Confidence:** HIGH
**Affected:** common (shared utilities)
**Evidence:** No test files in apps/common/
**Runtime impact:** Shared enums, managers, validators, and abstract models have no dedicated tests.

### FR-012: showcase App Has Zero Tests

**Severity:** LOW
**Confidence:** HIGH
**Affected:** showcase
**Evidence:** No test files in apps/showcase/
**Runtime impact:** UI component demos untested. Low risk since they render static content.

### FR-013: CI Pipeline Never Executed

**Severity:** MEDIUM
**Confidence:** HIGH
**Affected:** .github/workflows/ci.yml
**Evidence:** Workflow exists but never run.
**Runtime impact:** No automated test execution on PRs.

---

## Known Limitations (Not Defects)

| ID | Limitation | Impact |
|----|-----------|--------|
| KL-001 | Fake PSP only | Cannot process real payments |
| KL-002 | Fake notification providers | Cannot send real SMS/email/push |
| KL-003 | No production deployment config | Cannot deploy to production |
| KL-004 | Deadline expiry gated (disabled) | Payment deadlines don't auto-expire |
| KL-005 | Pre-service payment gated (disabled) | No escrow hold until payment |
| KL-006 | GIS disabled on Windows | No geospatial features in dev |
| KL-007 | No customer document verification | `VerificationDocument` has no customer owner FK; `CustomerProfile` has no `verification_status` field (confirmed 2026-07-15, Phase 1.1; unchanged by Phase 1.2). Phone/OTP verification is the current-phase mechanism for customers. See `quality/COMPLETION_BACKLOG.md` BG-016. |
| KL-008 | ~~No activation wiring / completion auto-recompute~~ — **RESOLVED (Phase 1.3, corrected in the PR #5 remediation)** | `ProfileActivationService.activate_caregiver()/activate_organization()` now calls `ActivationEligibilityService.evaluate()` and performs a real, audited, permission-gated `DRAFT -> ACTIVE` transition (BG-018, ARCHITECTURE_DECISION_LOG ADM-016). `profile.status` is the sole source of truth for current activation state — an earlier version of this fix used `AuditLog` existence as the activation signal, which was corrected before merge (PR #5 review). `ProfileCompletionService` is now the single deterministic source for completion percentage. Superseded item: profile `verification_status` roll-up itself is IMPLEMENTED (Phase 1.2), not a limitation. |
| KL-009 | No automatic deactivation on verification becoming invalid/expired | An already-ACTIVE profile's `status` is not automatically walked back when a required document later expires or a review is reversed — `ActivationEligibilityService.evaluate()` correctly reports `eligible=False` again, but nothing acts on that for an already-active profile. Explicitly deferred per Phase 1.3 task governance (no suspension/revalidation workflow exists to hook it into). See `quality/COMPLETION_BACKLOG.md` BG-019. |
| KL-010 | ~~Caregiver gallery~~ delivered (Sprint 2.2); ~~credentials/skills/experience presentation~~ delivered (Sprint 2.3); ~~availability~~ delivered (Sprint 2.4); financial overview, orders + history still not implemented | Roadmap Phase 2's full scope; Phase 2.1 (2026-07-15) delivered the foundation slice (skills, experience, verified-credential summary, corrected public-profile eligibility); Sprint 2.2 (2026-07-15) delivered the caregiver gallery/media portfolio; Sprint 2.3 (2026-07-15) delivered precise verification badges, skill/experience visibility management, an expiring-soon owner-facing credential state, and derived professional highlights; Sprint 2.4 (2026-07-15) delivered weekly working-hour intervals with overlap validation, time-off management, a canonical structured availability evaluator, and a privacy-safe public availability summary. Extended financial overview/orders + history (Sprint 2.5) remains open. See `quality/COMPLETION_BACKLOG.md` BG-021. |
| KL-011 | ~~Directory/home-page caregiver listings do not require verification/account-active~~ — **RESOLVED (BG-022, 2026-07-15)** | `verification_status == VERIFIED` and account `is_active` are now part of the single canonical `common.is_publicly_visible_attrs()` rule, applied uniformly by the caregiver directory, home-page listings, and the single profile detail page — no more divergence between "found in a listing" and "profile page loads." See `quality/COMPLETION_BACKLOG.md` BG-022 and `ARCHITECTURE_DECISION_LOG.md` ADM-017's second remediation note. |
| ~~KL-012 (pre-remediation)~~ | ~~Pre-existing per-candidate query cost in directory ranking/card-building (not eligibility)~~ — **RESOLVED (Sprint 2.6 PR #11 remediation, 2026-07-15)** | Discovered during BG-022's query-count verification (2026-07-15): `DiscoveryRankingService.rank()` issued one `availability_capacity_rule` query per candidate, and `CaregiverDirectoryService._build_card()` issued one `reviews_reputation_snapshot` query and one `orders_order` completed-jobs count query per card — pre-existing, unrelated to the BG-022 eligibility fix (which was already confirmed O(1)). Initially deferred as out of BG-022's and Sprint 2.6's own initial scope; a follow-up architecture review found this inconsistent with Sprint 2.6's own "query behavior is bounded" and "no unresolved Phase 2 blocker" acceptance criteria and required it resolved inside PR #11. **Fixed:** three independent per-candidate query sources batched — `CapacityService.bulk_is_capacity_exceeded()` (new, `apps.availability`) replaces the per-candidate `is_capacity_exceeded()` call inside `DiscoveryRankingService._score()`; `SupplierSearchService._filter_by_city()` now calls the pre-existing `resolve_supplier_entities_bulk()` instead of the per-candidate `resolve_supplier_entity()`; `CaregiverDirectoryService._build_card()` now consumes precomputed `rating_summaries_bulk()`/`completed_jobs_counts_bulk()` maps (both new) instead of querying per built card. Ranking formula, scoring weights, sort order, pagination, and public-visibility policy are all unchanged — only the underlying query pattern changed. Directory/search/home query counts are now fully flat from 1 to 100+ matching candidates (measured: directory 16, filtered search 17, home featured 17 — constant at every candidate count tested). Single-supplier call sites (`provider_portal`/`organization_portal`'s own capacity display, `resolve_supplier_entity()` elsewhere) were left unchanged — only the many-candidates code paths were batched. See `traceability/ARCHITECTURE_DECISION_LOG.md` ADM-022's remediation note. |
| ~~KL-013 (pre-remediation)~~ | ~~Gallery physical-file deletion ran before the database row deletion, inside the same transaction~~ — **RESOLVED (PR #7 review, 2026-07-15)** | `CaregiverGalleryService.remove_item()` originally deleted the stored file first, then the row — a later rollback of that (or an enclosing) transaction would have left a live row pointing at an already-deleted file, since filesystem operations aren't transactional. Fixed: the row is deleted first; physical deletion is scheduled via `transaction.on_commit()`, which Django discards entirely if the transaction rolls back. See `ARCHITECTURE_DECISION_LOG.md` ADM-018's remediation note. |
| KL-014 | Gallery orphan-file cleanup/retry not automated | If a post-commit physical file deletion itself fails (storage/filesystem error), the failure is logged (`CaregiverGalleryService._delete_stored_file()`), not retried — the database row stays deleted either way (correct, safe outcome), but the orphaned file itself is not automatically swept. No cleanup-job/retry infrastructure exists anywhere in this repository to hook this into (recorded, not built, per the PR #7 remediation's explicit "do not implement a broad background-cleanup subsystem" scope limit). Detectable via the `apps.accounts.services.caregiver_gallery_service` error log. |
| KL-015 | Gallery image decoded-dimension limits are fixed constants, not tenant-configurable | `MAX_IMAGE_WIDTH`/`MAX_IMAGE_HEIGHT`/`MAX_IMAGE_PIXELS` (`image_validation.py`) are explicit, hardcoded values (8000px/8000px/25M px) — matching every other numeric limit in this module's existing style (`MAX_IMAGE_BYTES`, `MAX_GALLERY_ITEMS_PER_CAREGIVER`). No product requirement or existing repo convention (e.g. `RequiredDocumentPolicy`'s `ConfigResolver`) calls for these to vary per tenant. Not a defect — a deliberate simplicity choice, recorded per `ARCHITECTURE_DECISION_LOG.md` ADM-018. |
| KL-016 | `CaregiverSkill.name` is free-text, no catalog/normalization | Two caregivers (or the same caregiver over time) can record spelling variants of the same skill (e.g. "پرستاری سالمندان" vs "مراقبت از سالمندان") as distinct rows — nothing merges them for search/filter purposes. Confirmed, not fixed, in Sprint 2.3 (2026-07-15): the model was deliberately kept free-text in Phase 2.1 (`ARCHITECTURE_DECISION_LOG.md` ADM-017) and Sprint 2.3's own governance explicitly warned against "silently redesigning" it. A skill catalog/taxonomy table would resolve this but is a genuinely new modeling decision, not a UI-completion task — recorded as a future migration risk, not attempted here. |
| ~~KL-017 (pre-remediation)~~ | ~~No duplicate/overlap validation on weekly working windows~~ — **RESOLVED (Sprint 2.4, 2026-07-15)** | `AvailabilityMutationService.add_working_window()`/`update_working_window()` previously accepted an exact duplicate or a partially overlapping active window on the same day for the same supplier with no rejection — the only validation was `start_time < end_time`. Fixed: a new `_validate_no_overlap()` check refuses both cases, excluding disabled windows on both sides. See `ARCHITECTURE_DECISION_LOG.md` ADM-020 Decision 3. |
| KL-018 | Per-caregiver time zone is not modeled | Every availability evaluation resolves through the single platform-wide `settings.TIME_ZONE` (`Asia/Tehran`) — a caregiver physically located elsewhere has their schedule interpreted against the platform default, not their own local time. Confirmed, not fixed, in Sprint 2.4 (2026-07-15) per that sprint's own explicit governance ("use the existing tenant/platform time-zone and document the limitation... do not invent a second time-zone source"). See `quality/COMPLETION_BACKLOG.md` BG-024. |
| ~~KL-019 (pre-remediation)~~ | ~~KL-017's overlap check was not concurrency-safe~~ — **RESOLVED (PR #9 review, 2026-07-15)** | KL-017's `_validate_no_overlap()` was a plain, unlocked `SELECT`, and `add_working_window()` took no lock at all before its check-then-insert — under PostgreSQL READ COMMITTED, two concurrent transactions creating overlapping windows for the same supplier/day could both read "no conflict" before either committed, then both insert (`transaction.atomic` alone does not serialize concurrent reads). `update_working_window()`'s pre-existing `select_for_update()` on the window row being updated did not close the gap either — it locks no row a concurrent `add_working_window()` touches, and two concurrent updates to two *different* windows of the same supplier/day each lock a different row. This was deferred as "multi-threaded concurrency race testing... not written" in the initial Sprint 2.4 report, and inspection during PR #9 review found it was a genuine implementation gap, not merely an untested-but-safe design. Fixed: both mutation methods now lock the owning `kernel.ServiceSupplier` row first, before any overlap check — mirroring `apps.accounts.services.caregiver_gallery_service.CaregiverGalleryService.add_item()`'s existing precedent for the same class of problem. Proven by 9 new `TransactionTestCase` tests in `apps.availability.tests.test_concurrency`, each asserting final database state. See `ARCHITECTURE_DECISION_LOG.md` ADM-020's remediation note. |
| KL-020 | No canonical bonus/penalty representation for caregivers | Confirmed by repository-wide inspection (Sprint 2.5, 2026-07-15): `apps.wallet.models.WalletTransactionType` has CREDIT/DEBIT/REFUND/PROMOTION/ADJUSTMENT/MANUAL, none carrying a bonus/penalty semantic; no dedicated adjustment/bonus/penalty model exists in `apps.commission` or elsewhere; the only other repository hits for "bonus"/"penalty" are an unrelated matching/discovery ranking-score concept and a comment referencing a never-built, reserved-for-a-future-PR cancellation-penalty engine. The Caregiver Professional Dashboard's financial overview documents this gap directly (`FinancialOverviewViewModel.bonus_penalty_note`) rather than inventing a CREDIT/DEBIT-based classification. See `quality/COMPLETION_BACKLOG.md` BG-026 and `ARCHITECTURE_DECISION_LOG.md` ADM-021 Decision 4. |
| KL-021 | **RESOLVED (Sprint 3.2, 2026-07-16).** `organization_profile.html` reused the caregiver-directory URL as its own SEO `page_url`/canonical target | Discovered during Sprint 2.6 (2026-07-15) while auditing `templates/public_site/caregiver_profile.html`'s identical defect (fixed in Sprint 2.6 — the caregiver page now passes its own `{% url 'public_site:caregiver-profile' %}` as both `page_url` and `canonical_url`). `templates/public_site/organization_profile.html` line 5 passed the generic `page_url="/find-an-organization/"` to `ui/components/public/seo_meta.html` instead of that organization's own detail URL. Deliberately left unfixed in Sprint 2.6 — organization-profile templates were out of that sprint's strict caregiver-public-profile-finalization scope. **Fixed in Sprint 3.2**, now this sprint's own scope: resolves `{% url 'public_site:organization-profile' supplier_id=profile.supplier_id %}` and passes it as both `page_url` and `canonical_url`, matching the caregiver page's own pattern exactly. See `quality/COMPLETION_BACKLOG.md` BG-030 and `ARCHITECTURE_DECISION_LOG.md` ADM-024 Decision 3. |
| KL-022 | No flash-message/error-surfacing framework for POST-action portal views | Confirmed by repository-wide inspection (Sprint 3.1, 2026-07-16): `django.contrib.messages` is used nowhere in `apps.organization_portal`/`apps.provider_portal`/`apps.portal`. Pre-existing POST-action buttons (`staff_approve_view`/`staff_suspend_view`) already had no error-surfacing path — a failed action just redirects back with no visible feedback. Sprint 3.1's new affiliation-lifecycle action views (`invite_caregiver_view`, `affiliation_request_approve_view`/`_reject_view`, `staff_terminate_view`, `invitation_cancel_view`, and the `provider_portal` company-page equivalents) deliberately match this existing convention (`except AccountsError: pass`, then redirect) rather than introducing flash messaging as a one-off for this sprint. Re-evaluated at the Phase 4 Closure Review (2026-07-17): still open, still explicitly cross-portal infrastructure — **does not block Phase 4 closure.** See `traceability/ARCHITECTURE_DECISION_LOG.md` ADM-023 and `quality/COMPLETION_BACKLOG.md` BG-029. |
| KL-023 | Favorites list fully materialized before Python-side pagination (engineering improvement, not a defect) | **Current behavior:** `CustomerFavoritesPresentationService.build_list_view()` (`apps/portal/services/favorites_service.py:44`) executes `favorites = list(favorites)`, materializing the customer's *entire* favorites queryset before offset/slice pagination in Python. Confirmed by direct code inspection during the Phase 4 Closure Review (2026-07-17). **Why non-blocking:** the query *count* stays bounded — `FavoritesViewQueryBudgetTest.test_query_count_bounded_at_representative_sizes` (`apps/portal/tests/test_favorites_view.py`) measures 0/1/5/20 favorites and asserts the query-count growth is `<= 8`, a small fixed bound, not per-row. Only row materialization/list-slicing scales with total favorite count, not query count. **Impact:** negligible at realistic scale — a customer's favorites list is self-curated preference data, not a system-generated unbounded collection; no production usage evidence suggests customers accumulate favorite counts large enough for this to matter. **Trigger for reconsideration:** real usage data showing customers with hundreds-plus favorites, or a future requirement to sort/filter server-side. **Not a security defect** — no additional data is exposed or made accessible; the same tenant/ownership-scoped queryset is materialized, just not lazily. **Not a Phase 4 closure blocker** — Phase 4's own acceptance criteria (favorites add/remove/list with tenant scoping and ownership checks; regression green) do not require server-side pagination. **Disposition:** deferred. **Not a Phase 5 prerequisite** — Phase 5 (Marketplace Order Workflow) does not touch Favorites. See `traceability/ARCHITECTURE_DECISION_LOG.md` ADM-028. |
| KL-024 | Favorites concurrency test uses a mocked `IntegrityError` recovery path, not a genuine parallel-transaction test (test-hardening improvement, not a defect) | **Current behavior:** `test_add_favorite_survives_integrity_error_race` (`apps/accounts/tests/test_favorites.py:131-149`) uses `unittest.mock.patch(..., side_effect=IntegrityError("simulated race"))` inside a plain `TestCase`, proving the catch-and-refetch branch executes correctly — not that two genuinely concurrent, separately-committed database transactions resolve to one row. Confirmed by direct code inspection during the Phase 4 Closure Review (2026-07-17). Contrast with this repository's own stronger precedent for genuinely concurrency-sensitive mutations: `apps.availability.tests.test_concurrency` and `apps.accounts.tests.test_affiliation_lifecycle`, both real `TransactionTestCase`s with separately-committed transactions. **Why non-blocking:** the production code path (`get_or_create()` + the DB `UniqueConstraint uq_customer_favorite_supplier`) is the same idempotent pattern already proven safe elsewhere in this codebase (`CaregiverSkillService`'s own duplicate-handling precedent); the DB constraint, not the test, is the real serialization boundary regardless of how it is tested. **Impact:** a favorites double-click race has no financial, tenant-isolation, or data-integrity consequence beyond one extra idempotent request — categorically lower stakes than the working-window-overlap or membership-activation races that justified the prior `TransactionTestCase` additions. **Trigger for reconsideration:** any future change to `FavoritesService.add_favorite()`'s concurrency handling, or a decision to touch this service for an unrelated reason (cheap to fix opportunistically then). **Not a data-integrity defect** — the DB `UniqueConstraint` is the real serialization boundary regardless of test technique, and no incorrect data state has ever been observed or demonstrated. **Not a Phase 4 closure blocker** — Phase 4's own acceptance criteria do not require a specific concurrency-test technique, only that add/remove/list behave correctly, which they do. **Disposition:** deferred, not a standalone remediation sprint. **Not a Phase 5 prerequisite.** See `traceability/ARCHITECTURE_DECISION_LOG.md` ADM-028. |
