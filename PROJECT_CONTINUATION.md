# PROJECT CONTINUATION

This root file is a pointer only. The authoritative, current continuation
document is:

**`project docs/02_PROJECT_CONTINUATION.md`**

Start reading at `project docs/00_START_HERE.md`.

Do not use `canonical docs/`, `mimo change/`, or anything under `_archive/`
as current authority — those paths are archived history.
