هدف

تعریف اصول امنیتی Module 03

Security-001

هر عملیات باید کاربر انجام‌دهنده را ثبت کند.

Security-002

هر تغییر باید Timestamp داشته باشد.

Security-003

تمام تغییرات مهم باید Immutable باشند.

Security-004

GPS فقط با رضایت و مجوز سیستم‌عامل استفاده شود.

Security-005

Timeline نباید قابل حذف باشد.

Security-006

Audit Log فقط توسط تیم مجاز Platform Owner قابل مشاهده باشد.

Security-007

هر مداخله دستی باید دلیل داشته باشد.

Security-008

تمام تغییرات دستی باید قبل و بعد از تغییر را ثبت کنند.

Security-009

هیچ Event نباید بدون شناسه یکتا ذخیره شود.

Security-010

همه Notificationها باید وضعیت ارسال خود را ثبت کنند.

Security-011

تمام تصمیمات سیستم باید قابل بازبینی باشند.